---
phase: 06-products-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/products/ProductsHero.tsx
  - src/components/sections/products/ProductCategoryGrid.tsx
  - src/app/products/page.tsx
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03

must_haves:
  truths:
    - "The Products page has a hero with 'Quality Materials, Delivered' headline matching the interior hero pattern"
    - "All seven product categories are displayed in a responsive grid with image placeholders and descriptions"
    - "Hovering a product card on desktop reveals a 'Contact for Pricing' overlay; on mobile the text is always visible"
  artifacts:
    - path: "src/components/sections/products/ProductsHero.tsx"
      provides: "Interior page hero for Products page"
      min_lines: 20
    - path: "src/components/sections/products/ProductCategoryGrid.tsx"
      provides: "7-card product grid with hover overlay"
      min_lines: 30
    - path: "src/app/products/page.tsx"
      provides: "Products page assembling hero and grid sections"
  key_links:
    - from: "src/components/sections/products/ProductCategoryGrid.tsx"
      to: "src/lib/data/products.ts"
      via: "import { products }"
      pattern: "import.*products.*from.*@/lib/data/products"
    - from: "src/app/products/page.tsx"
      to: "src/components/sections/products/ProductsHero.tsx"
      via: "import and render"
      pattern: "import ProductsHero"
---

<objective>
Create the Products page hero and product category grid with hover-reveal "Contact for Pricing" overlay.

Purpose: Establish the Products page with the hero banner and a visually rich grid of all 7 product categories, following the interior page hero pattern from About and Services pages.
Output: ProductsHero.tsx, ProductCategoryGrid.tsx, and initial page.tsx assembly with comment placeholders for Plan 02 sections.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/sections/services/ServicesHero.tsx
@src/components/sections/services/ServiceCardGrid.tsx
@src/lib/data/products.ts
@src/app/products/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProductsHero and ProductCategoryGrid components</name>
  <files>
    src/components/sections/products/ProductsHero.tsx
    src/components/sections/products/ProductCategoryGrid.tsx
  </files>
  <action>
Create the `products/` directory under `src/components/sections/`.

**ProductsHero.tsx** — Server Component (NO `'use client'`):
- Follow the exact interior hero pattern from ServicesHero/AboutHero: `py-32 md:py-40`, `bg-forest`, `overflow-hidden`
- Absolute-positioned `ImagePlaceholder` with label "Products — Retail yard with bulk landscape materials", `!aspect-auto h-full w-full !rounded-none !bg-forest-light/30`
- `GrainOverlay` for texture
- `h1` with "Quality Materials, Delivered" in `font-display text-4xl md:text-5xl lg:text-6xl text-cream`
- Subtitle: "Premium landscape supplies for every project — from aggregates and mulch to natural stone and seed." in `text-lg md:text-xl text-cream/80`
- Import `ImagePlaceholder` from `@/components/ui/ImagePlaceholder` and `GrainOverlay` from `@/components/ui/GrainOverlay`

**ProductCategoryGrid.tsx** — Server Component (NO `'use client'`):
- Import `{ products }` from `@/lib/data/products` and `ImagePlaceholder` from `@/components/ui/ImagePlaceholder` and `AnimatedSection` from `@/components/ui/AnimatedSection`
- Section with `py-20 md:py-28 px-6` on cream/default background
- Centered heading: `h2` "Our Products" in `font-display text-3xl md:text-4xl text-forest text-center mb-4`
- Optional subtitle below heading
- Responsive grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 md:gap-8`
- Each card wrapped in `AnimatedSection` with `delay={index * 0.1}` for staggered entrance
- Card structure (CSS-only hover, NO `'use client'`):
  - Outer div: `group relative rounded-lg overflow-hidden bg-white shadow-sm hover:shadow-md transition-shadow`
  - Image area: `ImagePlaceholder` with `label={product.image}` and `aspectRatio="square"`
  - Hover overlay: `absolute inset-0 flex items-center justify-center bg-forest/80 transition-opacity duration-300` with `md:opacity-0 md:group-hover:opacity-100` (always visible on mobile for touch accessibility, hover-reveal on desktop)
  - Overlay text: "Contact for Pricing" in `text-cream font-medium text-lg`
  - Content area: `p-4 md:p-5` with `h3` in `font-display text-lg md:text-xl text-forest mb-1` for name, `p` in `text-charcoal/70 text-sm leading-relaxed` for description
- Use `max-w-7xl mx-auto` container for the grid content
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Both files exist as Server Components (no 'use client' directive). Visually inspect import paths are correct.</verify>
  <done>ProductsHero renders the "Quality Materials, Delivered" hero banner. ProductCategoryGrid renders all 7 product cards with image placeholders, names, descriptions, and CSS-only hover "Contact for Pricing" overlay that is always visible on mobile.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble Products page with hero and grid</name>
  <files>src/app/products/page.tsx</files>
  <action>
Replace the placeholder content in `src/app/products/page.tsx` with the Products page assembly:

- Server Component (NO `'use client'`)
- Import `ProductsHero` from `@/components/sections/products/ProductsHero`
- Import `ProductCategoryGrid` from `@/components/sections/products/ProductCategoryGrid`
- Root element: `<div>` (NOT `<main>` — root layout owns `<main>`)
- Render `<ProductsHero />` then `<ProductCategoryGrid />`
- Add comment placeholders: `{/* Material Calculator — Plan 02 */}` and `{/* Retail Yard Callout — Plan 02 */}` below the grid
- Follow the exact same composition pattern as `services/page.tsx` and `about/page.tsx`
  </action>
  <verify>Run `npm run build` — build succeeds without errors. Navigate to `/products` in dev server — hero and product grid render correctly. Verify homepage `/` still works (ProductsBanner unchanged).</verify>
  <done>Products page at /products shows the hero and 7-card product grid. Homepage ProductsBanner is unaffected. Comment placeholders exist for Plan 02 sections.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` completes successfully
3. `/products` shows "Quality Materials, Delivered" hero with forest background, grain overlay, and image placeholder
4. All 7 product categories display in a responsive grid below the hero
5. On desktop, hovering a product card reveals "Contact for Pricing" overlay
6. `/` homepage still shows ProductsBanner correctly (no regression)
7. No `'use client'` directive in any new file (both are Server Components)
</verification>

<success_criteria>
Products page has a hero with the correct headline and a grid showing all 7 product categories with hover-reveal pricing prompt. No regressions on homepage. All new components are Server Components.
</success_criteria>

<output>
After completion, create `.planning/phases/06-products-page/06-01-SUMMARY.md`
</output>
