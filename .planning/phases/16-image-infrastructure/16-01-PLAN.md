---
phase: 16-image-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/images/heroes/.gitkeep
  - public/images/services/.gitkeep
  - public/images/products/.gitkeep
  - public/images/projects/.gitkeep
  - public/images/team/.gitkeep
  - src/lib/data/services.ts
  - src/lib/data/products.ts
  - src/lib/data/projects.ts
  - src/lib/data/about.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "A public/images/ directory exists with 5 subdirectories (heroes, services, products, projects, team)"
    - "Every service entry in services.ts has an image field containing a path under /images/services/"
    - "Every product entry in products.ts has an image field containing a path under /images/products/"
    - "Every project entry in projects.ts has an image field containing a path under /images/projects/"
    - "Every team member entry in about.ts has an image field containing a path under /images/team/"
    - "TypeScript compiles with zero type errors after data file changes"
  artifacts:
    - path: "public/images/heroes/.gitkeep"
      provides: "Empty heroes image directory tracked by git"
    - path: "public/images/services/.gitkeep"
      provides: "Empty services image directory tracked by git"
    - path: "public/images/products/.gitkeep"
      provides: "Empty products image directory tracked by git"
    - path: "public/images/projects/.gitkeep"
      provides: "Empty projects image directory tracked by git"
    - path: "public/images/team/.gitkeep"
      provides: "Empty team image directory tracked by git"
    - path: "src/lib/data/services.ts"
      provides: "Service data with real image paths"
      exports: ["services", "Service"]
      contains: "/images/services/"
    - path: "src/lib/data/products.ts"
      provides: "Product data with real image paths"
      exports: ["products", "Product"]
      contains: "/images/products/"
    - path: "src/lib/data/projects.ts"
      provides: "Project data with real image paths"
      exports: ["projects", "Project"]
      contains: "/images/projects/"
    - path: "src/lib/data/about.ts"
      provides: "Team member data with real image paths"
      exports: ["teamMembers", "TeamMember"]
      contains: "/images/team/"
  key_links:
    - from: "src/lib/data/services.ts"
      to: "public/images/services/"
      via: "image field path strings"
      pattern: "/images/services/[a-z-]+\\.jpg"
    - from: "src/lib/data/products.ts"
      to: "public/images/products/"
      via: "image field path strings"
      pattern: "/images/products/[a-z-]+\\.jpg"
    - from: "src/lib/data/projects.ts"
      to: "public/images/projects/"
      via: "image field path strings"
      pattern: "/images/projects/[a-z-]+\\.jpg"
    - from: "src/lib/data/about.ts"
      to: "public/images/team/"
      via: "image field path strings"
      pattern: "/images/team/[a-z-]+\\.jpg"
---

<objective>
Create the image directory structure and update all data models to use real image paths instead of label strings.

Purpose: Establish the infrastructure that all subsequent image phases (17-20) depend on — directory layout for image files and data model paths that next/image components will reference.

Output: 5 image subdirectories under public/images/ and 4 updated data files with image path fields.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/data/services.ts
@src/lib/data/products.ts
@src/lib/data/projects.ts
@src/lib/data/about.ts
@src/components/ui/ImagePlaceholder.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public/images/ directory structure with .gitkeep files</name>
  <files>
    public/images/heroes/.gitkeep
    public/images/services/.gitkeep
    public/images/products/.gitkeep
    public/images/projects/.gitkeep
    public/images/team/.gitkeep
  </files>
  <action>
Create the directory tree under public/images/ with 5 subdirectories. Each subdirectory gets an empty .gitkeep file so git tracks the empty directories.

Directory structure to create:
```
public/images/
  heroes/      — for hero background images (Phase 17)
  services/    — for service card images (Phase 18)
  products/    — for product card images (Phase 18)
  projects/    — for gallery project images (Phase 19)
  team/        — for team member portraits (Phase 18)
```

Use `mkdir -p` for directory creation and `touch` for .gitkeep files. Do NOT create any placeholder image files — only .gitkeep files to hold the directories in git.
  </action>
  <verify>
Run: `ls -la public/images/heroes/.gitkeep public/images/services/.gitkeep public/images/products/.gitkeep public/images/projects/.gitkeep public/images/team/.gitkeep`

All 5 .gitkeep files must exist. No other files should be in these directories.
  </verify>
  <done>
public/images/ exists with exactly 5 subdirectories (heroes, services, products, projects, team), each containing a .gitkeep file and nothing else.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update data files to use image paths instead of label strings</name>
  <files>
    src/lib/data/services.ts
    src/lib/data/products.ts
    src/lib/data/projects.ts
    src/lib/data/about.ts
  </files>
  <action>
Update the `image` field in all 4 data files to contain a file path under `/images/` instead of the current label string. The `imageLabel` field (in services.ts) remains unchanged — it serves a different purpose (alt text for future next/image migration).

**Naming convention:** Use the entry's `id` as the filename with `.jpg` extension. Prefix with the subdirectory.

**services.ts** — Update each service's `image` field:
- `landscape-design-build` → `'/images/services/landscape-design-build.jpg'`
- `residential-maintenance` → `'/images/services/residential-maintenance.jpg'`
- `commercial-landscaping` → `'/images/services/commercial-landscaping.jpg'`
- `municipal-projects` → `'/images/services/municipal-projects.jpg'`
- `irrigation-lighting` → `'/images/services/irrigation-lighting.jpg'`
- `snow-removal` → `'/images/services/snow-removal.jpg'`

**products.ts** — Update each product's `image` field:
- `aggregates` → `'/images/products/aggregates.jpg'`
- `mulch` → `'/images/products/mulch.jpg'`
- `fertilizer` → `'/images/products/fertilizer.jpg'`
- `natural-stone` → `'/images/products/natural-stone.jpg'`
- `topsoil` → `'/images/products/topsoil.jpg'`
- `interlock` → `'/images/products/interlock.jpg'`
- `seed` → `'/images/products/seed.jpg'`

**projects.ts** — Update each project's `image` field:
- `riverside-patio-pergola` → `'/images/projects/riverside-patio-pergola.jpg'`
- `elora-commercial-grounds` → `'/images/projects/elora-commercial-grounds.jpg'`
- `natural-stone-retaining-wall` → `'/images/projects/natural-stone-retaining-wall.jpg'`
- `interlock-driveway-entrance` → `'/images/projects/interlock-driveway-entrance.jpg'`
- `garden-irrigation-system` → `'/images/projects/garden-irrigation-system.jpg'`
- `backyard-retreat-fergus` → `'/images/projects/backyard-retreat-fergus.jpg'`
- `fergus-garden-redesign` → `'/images/projects/fergus-garden-redesign.jpg'`
- `elora-plaza-maintenance` → `'/images/projects/elora-plaza-maintenance.jpg'`
- `outdoor-kitchen-patio` → `'/images/projects/outdoor-kitchen-patio.jpg'`
- `sports-field-irrigation` → `'/images/projects/sports-field-irrigation.jpg'`

**about.ts** — Update each team member's `image` field:
- `member-1` → `'/images/team/member-1.jpg'`
- `member-2` → `'/images/team/member-2.jpg'`
- `member-3` → `'/images/team/member-3.jpg'`
- `member-4` → `'/images/team/member-4.jpg'`

**Important:** The `image` field type remains `string` in all interfaces — no type changes needed. Only the field VALUES change from label strings to path strings. Do NOT modify the `imageLabel` field in services.ts. Do NOT add any new fields to interfaces.
  </action>
  <verify>
1. Run: `npx tsc --noEmit` — must exit 0 with no type errors.
2. Run: `grep -c '/images/' src/lib/data/services.ts` — should output 6
3. Run: `grep -c '/images/' src/lib/data/products.ts` — should output 7
4. Run: `grep -c '/images/' src/lib/data/projects.ts` — should output 10
5. Run: `grep -c '/images/' src/lib/data/about.ts` — should output 4
  </verify>
  <done>
All 27 entries across 4 data files (6 services + 7 products + 10 projects + 4 team members) have image fields containing valid paths under /images/{type}/{id}.jpg. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. Directory structure: `find public/images -type d | sort` returns exactly 6 directories (images/ + 5 subdirectories)
2. Data integrity: `grep -r '/images/' src/lib/data/ | wc -l` returns 27 (6+7+10+4)
3. Type safety: `npx tsc --noEmit` exits with code 0
4. No regressions: `npm run build` completes successfully (the site will show broken image placeholders until real images are added in later phases — that is expected)
</verification>

<success_criteria>
- public/images/ directory exists with heroes/, services/, products/, projects/, team/ subdirectories
- All 27 data entries across services.ts, products.ts, projects.ts, about.ts have image paths (not label strings)
- Image paths follow consistent convention: /images/{type}/{id}.jpg
- TypeScript compiles with zero errors
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/16-image-infrastructure/16-01-SUMMARY.md`
</output>
