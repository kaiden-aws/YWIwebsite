---
phase: 20-component-migration-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/services/ServicesHero.tsx
  - src/components/sections/services/ServiceCardGrid.tsx
  - src/components/sections/ServicesPreview.tsx
  - src/components/sections/AboutTeaser.tsx
  - src/components/sections/about/AboutHero.tsx
  - src/components/sections/about/TeamSection.tsx
  - src/components/sections/HeroParallax.tsx
  - src/components/sections/ProjectShowcase.tsx
  - src/components/sections/ProductsBanner.tsx
  - src/components/sections/gallery/GalleryLightbox.tsx
  - src/components/sections/gallery/GalleryHero.tsx
  - src/components/sections/gallery/GalleryGrid.tsx
  - src/components/sections/products/ProductsHero.tsx
  - src/components/sections/products/ProductCategoryGrid.tsx
  - src/components/sections/contact/ContactHero.tsx
  - src/components/ui/ImagePlaceholder.tsx
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Zero imports of ImagePlaceholder exist anywhere in the codebase"
    - "The file src/components/ui/ImagePlaceholder.tsx does not exist"
    - "Every next/image instance uses an appropriate sizes prop"
    - "Every next/image instance has a descriptive alt attribute"
    - "The site builds with zero TypeScript errors"
  artifacts:
    - path: "src/components/ui/ImagePlaceholder.tsx"
      provides: "DELETED — must not exist"
      contains: null
  key_links:
    - from: "all 15 component files"
      to: "next/image"
      via: "import Image from 'next/image'"
      pattern: "import Image from 'next/image'"
---

<objective>
Audit all next/image usages for proper optimization props, delete the ImagePlaceholder component, and verify a clean build.

Purpose: Phases 17-19 already replaced all ImagePlaceholder usages with next/image. This plan performs the final audit of image props (sizes, alt, priority), deletes the now-unused ImagePlaceholder.tsx file, and confirms the site builds cleanly — completing the v1.2 Real Images milestone.

Output: All 15 image components verified with correct props, ImagePlaceholder.tsx deleted, clean TypeScript build confirmed.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/components/ui/ImagePlaceholder.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix next/image props across all 15 components</name>
  <files>
    src/components/sections/services/ServicesHero.tsx
    src/components/sections/services/ServiceCardGrid.tsx
    src/components/sections/ServicesPreview.tsx
    src/components/sections/AboutTeaser.tsx
    src/components/sections/about/AboutHero.tsx
    src/components/sections/about/TeamSection.tsx
    src/components/sections/HeroParallax.tsx
    src/components/sections/ProjectShowcase.tsx
    src/components/sections/ProductsBanner.tsx
    src/components/sections/gallery/GalleryLightbox.tsx
    src/components/sections/gallery/GalleryHero.tsx
    src/components/sections/gallery/GalleryGrid.tsx
    src/components/sections/products/ProductsHero.tsx
    src/components/sections/products/ProductCategoryGrid.tsx
    src/components/sections/contact/ContactHero.tsx
  </files>
  <action>
Read each of the 15 component files that use next/image. For each one, verify and fix if needed:

1. **sizes prop**: Every Image component must have a `sizes` prop.
   - Hero components (HeroParallax, ServicesHero, AboutHero, ProductsHero, GalleryHero, ContactHero): `sizes="100vw"`
   - Grid card components (ServiceCardGrid, ProductCategoryGrid, ProductsBanner): responsive breakpoint sizes appropriate for their grid layout
   - Gallery components (GalleryGrid, GalleryLightbox): responsive sizes appropriate for masonry/lightbox layout
   - Other components (ServicesPreview, AboutTeaser, TeamSection, ProjectShowcase): responsive sizes matching their layout

2. **alt attribute**: Every Image must have a descriptive alt. Check that:
   - Hero images have descriptive scene text (not just "hero" or "image")
   - Card images use the item's name or descriptive label (not a file path string)
   - Team portraits include name and role
   - Gallery images use project name

3. **priority prop**: Only the home page hero (HeroParallax) should have `priority={true}` since it is the above-fold hero. All other images should NOT have priority (they are below-fold or on interior pages).

4. **fill prop**: Background-style images (heroes, cards with aspect-ratio wrappers) should use `fill={true}` with `className="object-cover"`.

If any component is missing or has an incorrect prop value, fix it. If all props are correct, no changes needed to that file.

Document findings: create a checklist of all 15 components noting which were correct and which needed fixes.
  </action>
  <verify>
Run `grep -rn 'sizes=' src/components/sections/` and verify all 15 Image usages have sizes prop.
Run `grep -rn 'alt=' src/components/sections/` and verify all 15 have descriptive alt text (not empty strings or file paths).
Run `grep -rn 'priority' src/components/sections/` and verify only HeroParallax has priority={true}.
  </verify>
  <done>All 15 next/image instances have appropriate sizes prop, descriptive alt attribute, and correct priority usage. No Image component is missing required optimization props.</done>
</task>

<task type="auto">
  <name>Task 2: Delete ImagePlaceholder component and verify clean build</name>
  <files>
    src/components/ui/ImagePlaceholder.tsx
  </files>
  <action>
1. First confirm zero imports of ImagePlaceholder exist in any source file:
   `grep -rn "ImagePlaceholder" src/ --include="*.tsx" --include="*.ts"` — should only show the component definition file itself, no imports.

2. Delete the file `src/components/ui/ImagePlaceholder.tsx`.

3. Run `npx next build` (or the project's build command) and confirm:
   - Zero TypeScript errors
   - Zero build warnings related to missing ImagePlaceholder
   - Build completes successfully

4. Run a final comprehensive check:
   - `grep -rn "ImagePlaceholder" src/` returns zero results
   - All pages that previously used ImagePlaceholder still reference next/image correctly
  </action>
  <verify>
Run `ls src/components/ui/ImagePlaceholder.tsx 2>&amp;1` — should show "No such file or directory".
Run `grep -rn "ImagePlaceholder" src/` — should return zero results.
Run `npx next build` — should complete with zero errors.
  </verify>
  <done>ImagePlaceholder.tsx is deleted, zero references remain in source code, and the site builds successfully with zero TypeScript errors.</done>
</task>

</tasks>

<verification>
1. `grep -rn "import.*ImagePlaceholder" src/` returns zero results
2. `ls src/components/ui/ImagePlaceholder.tsx` shows file does not exist
3. `grep -rn "sizes=" src/components/sections/ | wc -l` returns at least 15 (one per Image component)
4. `grep -rn "alt=" src/components/sections/ | wc -l` returns at least 15
5. `npx next build` completes with zero errors
</verification>

<success_criteria>
- Zero imports of ImagePlaceholder exist anywhere in the codebase
- src/components/ui/ImagePlaceholder.tsx does not exist
- Every next/image instance uses an appropriate sizes prop (100vw for heroes, responsive breakpoints for grids)
- Every next/image instance has a descriptive alt attribute (not empty, not a file path)
- Only HeroParallax uses priority={true}
- The site builds with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-component-migration-cleanup/20-01-SUMMARY.md`
</output>
