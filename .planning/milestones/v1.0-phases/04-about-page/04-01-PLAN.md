---
phase: 04-about-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/about.ts
  - src/components/sections/about/AboutHero.tsx
  - src/components/sections/about/CompanyStory.tsx
  - src/components/sections/about/ValuesGrid.tsx
  - src/app/about/page.tsx
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03

must_haves:
  truths:
    - "The About page displays a hero section with 'Our Story' headline, a background image placeholder, and grain overlay on a forest green background"
    - "Scrolling below the hero reveals a company story section with 3-4 paragraphs about Fergus roots, community ties, and residential/commercial/municipal client range"
    - "A values grid shows four cards (Quality, Integrity, Community, Craftsmanship) each with a lucide-react icon, name, and description"
  artifacts:
    - path: "src/lib/data/about.ts"
      provides: "Typed data arrays for values, team members, and differentiators"
      contains: "export const values"
    - path: "src/components/sections/about/AboutHero.tsx"
      provides: "Interior page hero with Our Story headline and image placeholder"
      contains: "Our Story"
    - path: "src/components/sections/about/CompanyStory.tsx"
      provides: "Company narrative section about Fergus roots"
      contains: "AnimatedSection"
    - path: "src/components/sections/about/ValuesGrid.tsx"
      provides: "4-value grid with lucide-react icons"
      contains: "Award"
    - path: "src/app/about/page.tsx"
      provides: "Server Component page assembling about sections"
      contains: "AboutHero"
  key_links:
    - from: "src/components/sections/about/ValuesGrid.tsx"
      to: "src/lib/data/about.ts"
      via: "import { values }"
      pattern: "import.*values.*from.*lib/data/about"
    - from: "src/app/about/page.tsx"
      to: "src/components/sections/about/"
      via: "section component imports"
      pattern: "import.*from.*sections/about"
---

<objective>
Create the About page data layer, hero section, company story, and values grid — establishing the page structure and first three visual sections.

Purpose: Deliver the About page's trust-building foundation: a professional hero banner that orients the visitor, a narrative section that communicates Fergus roots and client breadth, and a values grid that signals what the company stands for.

Output: `lib/data/about.ts` with all About page data, three section components (`AboutHero`, `CompanyStory`, `ValuesGrid`), and `app/about/page.tsx` updated to compose them (with placeholder imports for Plan 02 sections).
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-about-page/04-RESEARCH.md
@src/app/page.tsx
@src/components/ui/AnimatedSection.tsx
@src/components/ui/ImagePlaceholder.tsx
@src/components/ui/GrainOverlay.tsx
@src/lib/data/services.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create about data file and AboutHero + CompanyStory sections</name>
  <files>
    src/lib/data/about.ts
    src/components/sections/about/AboutHero.tsx
    src/components/sections/about/CompanyStory.tsx
  </files>
  <action>
**1. Create `src/lib/data/about.ts`** with all About page data (this file serves Plans 01 and 02):

Define and export these typed interfaces and arrays:

```typescript
import type { LucideIcon } from 'lucide-react'
import { Award, Shield, Users, Hammer } from 'lucide-react'
```

- `Value` interface: `{ icon: LucideIcon; name: string; description: string }`
- `TeamMember` interface: `{ id: string; name: string; role: string; image: string }`
- `Differentiator` interface: `{ title: string; description: string }`

Export `values: Value[]` — 4 items:
  - Award icon, "Quality", description about premium materials and time-tested techniques
  - Shield icon, "Integrity", description about honest pricing and clear communication
  - Users icon, "Community", description about Fergus roots and Centre Wellington commitment
  - Hammer icon, "Craftsmanship", description about care and attention from design through final walkthrough

Export `teamMembers: TeamMember[]` — 4 placeholder members:
  - member-1: "Team Member", "Owner / Lead Designer", image label "Team — Owner headshot"
  - member-2: "Team Member", "Project Manager", image label "Team — Project manager headshot"
  - member-3: "Team Member", "Lead Installer", image label "Team — Lead installer headshot"
  - member-4: "Team Member", "Irrigation Specialist", image label "Team — Irrigation specialist headshot"

Export `differentiators: Differentiator[]` — 5 items:
  - "Knowledgeable Crew" — years of hands-on experience across residential, commercial, municipal
  - "Friendly, Approachable Team" — great work starts with great communication
  - "Top Quality Products" — premium materials from retail yard, same products used on own projects
  - "Design-to-Build Service" — one team from consultation through installation
  - "Licensed Backflow Preventor Testing" — MUST explain: holds required certification for backflow preventor testing on irrigation systems, credential many landscapers lack, keeps irrigation projects compliant with Ontario regulations

**2. Create `src/components/sections/about/AboutHero.tsx`** — Server Component (NO `'use client'`):

- Import `ImagePlaceholder` from `@/components/ui/ImagePlaceholder` and `GrainOverlay` from `@/components/ui/GrainOverlay`
- Render a `<section>` with: `relative py-32 md:py-40 px-6 bg-forest overflow-hidden`
  - The `py-32` provides 128px top padding to clear the ~64-80px sticky header
- Inside: absolute-positioned div (`absolute inset-0`) containing `ImagePlaceholder` with:
  - `label="About — YWI team working on a landscaping project"`
  - `className="!aspect-auto h-full w-full !rounded-none !bg-forest-light/30"` (stretch to fill, override default aspect/rounding)
- `GrainOverlay` for texture consistency
- Content div (`relative z-10 max-w-4xl mx-auto text-center`):
  - `<h1>` with `font-display text-4xl md:text-5xl lg:text-6xl text-cream mb-4` containing "Our Story"
  - `<p>` with `text-lg md:text-xl text-cream/80 max-w-2xl mx-auto` containing a subline about Fergus roots and building outdoor spaces

**3. Create `src/components/sections/about/CompanyStory.tsx`** — Server Component (NO `'use client'`):

- Import `AnimatedSection` from `@/components/ui/AnimatedSection`
- Render a `<section>` with: `py-20 md:py-28 px-6` (cream background — default, no bg class needed)
- Content container: `max-w-4xl mx-auto`
- Wrap content in `AnimatedSection`
- Section heading: `<h2>` with `font-display text-3xl md:text-4xl text-forest mb-8` — something like "Rooted in Fergus, Built on Trust"
- 3-4 paragraphs of placeholder text (each `<p>` with `text-charcoal/80 leading-relaxed mb-6`), covering:
  1. Founding story — started in Fergus, grew from a passion for transforming outdoor spaces
  2. Growth — evolved from residential projects to serving commercial properties and municipal contracts
  3. Community — deep ties to Centre Wellington, neighbours they see at the hardware store
  4. Retail yard — opened the materials yard at 6470 Beatty Line N to serve both pros and homeowners
- Use `max-w-3xl` or similar on the prose block for comfortable reading width
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors. Verify files exist: `src/lib/data/about.ts`, `src/components/sections/about/AboutHero.tsx`, `src/components/sections/about/CompanyStory.tsx`.
  </verify>
  <done>
`about.ts` exports typed `values`, `teamMembers`, and `differentiators` arrays. `AboutHero` renders a forest-green hero with "Our Story" h1, image placeholder, and grain overlay. `CompanyStory` renders a narrative section with 3-4 paragraphs about Fergus roots wrapped in AnimatedSection.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ValuesGrid section and assemble page.tsx</name>
  <files>
    src/components/sections/about/ValuesGrid.tsx
    src/app/about/page.tsx
  </files>
  <action>
**1. Create `src/components/sections/about/ValuesGrid.tsx`** — Server Component (NO `'use client'`):

- Import `AnimatedSection` from `@/components/ui/AnimatedSection`
- Import `{ values }` from `@/lib/data/about`
- Render a `<section>` with: `py-20 md:py-28 px-6 bg-white`
- Content container: `max-w-7xl mx-auto`
- Section heading: `<h2>` with `font-display text-3xl md:text-4xl text-forest mb-4 text-center` — "What We Stand For"
- Optional subtitle: `<p>` with `text-charcoal/60 text-center mb-12 max-w-2xl mx-auto` — brief line about values guiding every project
- Grid: `grid grid-cols-2 md:grid-cols-4 gap-6 md:gap-8`
- Map over `values` array, each item wrapped in `AnimatedSection` with `delay={index * 0.1}`:
  - Card container: `text-center p-6`
  - Icon container: `inline-flex items-center justify-center w-14 h-14 rounded-full bg-sage/20 mb-4`
  - Render `<value.icon className="w-7 h-7 text-forest" />` inside the container
  - Value name: `<h3>` with `font-display text-lg text-forest mb-2`
  - Description: `<p>` with `text-charcoal/70 text-sm leading-relaxed`

**2. Replace `src/app/about/page.tsx`** — Server Component (NO `'use client'`):

Replace the entire placeholder content. Follow the exact pattern from `src/app/page.tsx`:

```typescript
import AboutHero from '@/components/sections/about/AboutHero'
import CompanyStory from '@/components/sections/about/CompanyStory'
import ValuesGrid from '@/components/sections/about/ValuesGrid'

export default function AboutPage() {
  return (
    <div>
      <AboutHero />
      <CompanyStory />
      <ValuesGrid />
      {/* TeamSection and WhyChooseUs — added in Plan 04-02 */}
    </div>
  )
}
```

Use `<div>` as root (NOT `<main>` or `<section>`) per project convention — root layout owns `<main>`.

Include comment placeholders for Plan 02 sections (TeamSection, WhyChooseUs) so the next plan knows where to add them.
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors. Run `npm run build` — build succeeds. Visit `/about` in the dev server — hero with "Our Story" visible, company story section readable, values grid shows 4 cards with icons.
  </verify>
  <done>
`ValuesGrid` renders a 4-column responsive grid with lucide-react icons (Award, Shield, Users, Hammer), value names, and descriptions. `page.tsx` is a Server Component assembling AboutHero, CompanyStory, and ValuesGrid with comment placeholders for Plan 02 sections. The About page renders all three sections in the browser with correct backgrounds (forest hero, cream story, white values).
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no type errors across all new files
2. `npm run build` succeeds — page server-renders without error
3. `/about` route shows: forest-green hero with "Our Story" h1 and image placeholder, company story with narrative paragraphs, values grid with 4 icon cards
4. View page source confirms server-rendered HTML (not client-side shell) — all text content present in source
5. No `'use client'` in any new file except pre-existing `AnimatedSection`
6. Hero headline clears the sticky header (py-32 provides sufficient top padding)
7. Section backgrounds alternate: forest (hero) → cream (story) → white (values)
</verification>

<success_criteria>
- About page hero displays "Our Story" with image placeholder and grain overlay on forest green background
- Company story section has 3-4 readable paragraphs about Fergus roots, growth, community, and retail yard
- Values grid shows Quality (Award), Integrity (Shield), Community (Users), Craftsmanship (Hammer) with descriptions
- All sections use established spacing convention (py-20 md:py-28 px-6)
- `about.ts` data file includes all data needed for Plan 02 (teamMembers, differentiators)
- Page is entirely server-rendered — zero new `'use client'` components
</success_criteria>

<output>
After completion, create `.planning/phases/04-about-page/04-01-SUMMARY.md`
</output>
