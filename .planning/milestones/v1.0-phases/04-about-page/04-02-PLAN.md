---
phase: 04-about-page
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/sections/about/TeamSection.tsx
  - src/components/sections/about/WhyChooseUs.tsx
  - src/app/about/page.tsx
autonomous: true
requirements:
  - ABOUT-04
  - ABOUT-05

must_haves:
  truths:
    - "A team section displays four placeholder member cards, each with a portrait image placeholder, name, and role"
    - "A 'Why Choose Yard Weasels' section lists five differentiators including 'Licensed Backflow Preventor Testing' with an explanation of the credential"
    - "All five About page sections render in order: hero, story, values, team, differentiators"
  artifacts:
    - path: "src/components/sections/about/TeamSection.tsx"
      provides: "Team member card grid with portrait placeholders"
      contains: "teamMembers"
    - path: "src/components/sections/about/WhyChooseUs.tsx"
      provides: "Five differentiators with icons and descriptions"
      contains: "Licensed Backflow Preventor Testing"
    - path: "src/app/about/page.tsx"
      provides: "Complete About page with all 5 sections"
      contains: "WhyChooseUs"
  key_links:
    - from: "src/components/sections/about/TeamSection.tsx"
      to: "src/lib/data/about.ts"
      via: "import { teamMembers }"
      pattern: "import.*teamMembers.*from.*lib/data/about"
    - from: "src/components/sections/about/WhyChooseUs.tsx"
      to: "src/lib/data/about.ts"
      via: "import { differentiators }"
      pattern: "import.*differentiators.*from.*lib/data/about"
    - from: "src/app/about/page.tsx"
      to: "src/components/sections/about/"
      via: "all 5 section imports"
      pattern: "import.*from.*sections/about"
---

<objective>
Add the team section and "Why Choose Yard Weasels" differentiator section to complete the About page — fulfilling all five phase requirements.

Purpose: Complete the About page's trust-building narrative by showing who is on the team (faces and roles build personal connection) and explicitly listing competitive advantages that differentiate YWI from other landscapers (especially the licensed backflow preventor testing credential).

Output: `TeamSection` and `WhyChooseUs` section components, plus the final `page.tsx` with all five sections assembled.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-about-page/04-RESEARCH.md
@.planning/phases/04-about-page/04-01-SUMMARY.md
@src/lib/data/about.ts
@src/app/about/page.tsx
@src/components/ui/AnimatedSection.tsx
@src/components/ui/ImagePlaceholder.tsx
@src/components/ui/GrainOverlay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TeamSection and WhyChooseUs section components</name>
  <files>
    src/components/sections/about/TeamSection.tsx
    src/components/sections/about/WhyChooseUs.tsx
  </files>
  <action>
**1. Create `src/components/sections/about/TeamSection.tsx`** — Server Component (NO `'use client'`):

- Import `AnimatedSection` from `@/components/ui/AnimatedSection`
- Import `ImagePlaceholder` from `@/components/ui/ImagePlaceholder`
- Import `{ teamMembers }` from `@/lib/data/about`
- Render a `<section>` with: `py-20 md:py-28 px-6` (cream background — default, no bg class)
- Content container: `max-w-7xl mx-auto`
- Section heading: `<h2>` with `font-display text-3xl md:text-4xl text-forest mb-4 text-center` — "Meet the Team"
- Optional subtitle: `<p>` with `text-charcoal/60 text-center mb-12 max-w-2xl mx-auto` — brief line about the people behind the projects
- Grid: `grid grid-cols-2 md:grid-cols-4 gap-6 md:gap-8`
- Map over `teamMembers`, each wrapped in `AnimatedSection` with `delay={index * 0.1}`:
  - Card: `text-center`
  - `ImagePlaceholder` with `label={member.image}`, `aspectRatio="portrait"`, `className="rounded-xl mb-4"`
  - Name: `<h3>` with `font-display text-lg text-forest`
  - Role: `<p>` with `text-charcoal/60 text-sm`

The 4 members fill the 4-column grid cleanly on desktop, and the 2-column grid on mobile shows 2 rows of 2 — no orphaned cards.

**2. Create `src/components/sections/about/WhyChooseUs.tsx`** — Server Component (NO `'use client'`):

- Import `AnimatedSection` from `@/components/ui/AnimatedSection`
- Import `GrainOverlay` from `@/components/ui/GrainOverlay`
- Import `{ CheckCircle }` from `lucide-react`
- Import `{ differentiators }` from `@/lib/data/about`
- Render a `<section>` with: `relative py-20 md:py-28 px-6 bg-forest overflow-hidden` (forest background for visual weight at page bottom)
- Add `GrainOverlay` for texture consistency with the hero
- Content container: `relative z-10 max-w-4xl mx-auto`
- Section heading: `<h2>` with `font-display text-3xl md:text-4xl text-cream mb-4 text-center` — "Why Choose Yard Weasels"
- Optional subtitle: `<p>` with `text-cream/70 text-center mb-12 max-w-2xl mx-auto` — brief line about what sets YWI apart
- Differentiator list: `space-y-6`
- Map over `differentiators`, each wrapped in `AnimatedSection` with `delay={index * 0.1}`:
  - Row layout: `flex gap-4 items-start`
  - `CheckCircle` icon: `className="w-6 h-6 text-terracotta flex-shrink-0 mt-1"` — terracotta accent for contrast on forest bg
  - Text container: `<div>`
    - Title: `<h3>` with `font-display text-lg text-cream mb-1`
    - Description: `<p>` with `text-cream/70 leading-relaxed`

CRITICAL: The "Licensed Backflow Preventor Testing" differentiator MUST have a description that explains what it is, why it matters (Ontario regulation compliance), and that YWI holds this credential while many competitors do not. This is a genuine competitive advantage, not just a bullet point.
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors. Verify files exist and contain expected imports from `@/lib/data/about`.
  </verify>
  <done>
`TeamSection` renders a responsive 4-column grid of team member cards with portrait ImagePlaceholders, names, and roles. `WhyChooseUs` renders 5 differentiators with CheckCircle icons on a forest-green textured background, with the backflow testing credential prominently explained.
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete About page.tsx with all five sections</name>
  <files>
    src/app/about/page.tsx
  </files>
  <action>
Update `src/app/about/page.tsx` to import and render `TeamSection` and `WhyChooseUs` below the existing three sections.

Replace the comment placeholders from Plan 01 with actual imports:

```typescript
import AboutHero from '@/components/sections/about/AboutHero'
import CompanyStory from '@/components/sections/about/CompanyStory'
import ValuesGrid from '@/components/sections/about/ValuesGrid'
import TeamSection from '@/components/sections/about/TeamSection'
import WhyChooseUs from '@/components/sections/about/WhyChooseUs'

export default function AboutPage() {
  return (
    <div>
      <AboutHero />
      <CompanyStory />
      <ValuesGrid />
      <TeamSection />
      <WhyChooseUs />
    </div>
  )
}
```

- Root element is `<div>` (NOT `<main>`) — root layout owns `<main>`
- No `'use client'` — this is a Server Component
- Section order produces this background rhythm: forest (hero) → cream (story) → white (values) → cream (team) → forest (why choose us)
- No data imports needed in page.tsx — each section component imports its own data
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors. Run `npm run build` — build succeeds. Visit `/about` in the dev server — all 5 sections render in order: hero, story, values grid, team cards, differentiators. View page source — full HTML is server-rendered, all text content present in source.
  </verify>
  <done>
`page.tsx` imports and renders all 5 About sections. The complete About page is navigable at `/about` with: (1) forest hero with "Our Story", (2) cream company story with Fergus narrative, (3) white values grid with 4 icon cards, (4) cream team section with 4 portrait cards, (5) forest differentiator section with 5 advantages including backflow testing. All content is server-rendered.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no type errors across all files
2. `npm run build` succeeds — complete page server-renders without error
3. `/about` route shows all 5 sections in order with correct background alternation
4. Team section shows exactly 4 cards in a clean grid (no orphaned cards)
5. "Why Choose Yard Weasels" section shows 5 differentiators with CheckCircle icons
6. "Licensed Backflow Preventor Testing" has a multi-sentence description explaining the credential
7. View page source confirms all content is server-rendered HTML
8. No `'use client'` directive in any new file
9. Section spacing follows convention: py-20 md:py-28 px-6
</verification>

<success_criteria>
- Team section displays 4 placeholder member cards with portrait photos, names, and roles in a responsive grid
- "Why Choose Yard Weasels" section lists all 5 differentiators with icons and descriptions
- Backflow preventor testing credential is explained (what it is, why it matters, that YWI holds it)
- All 5 sections render on `/about` in the correct order with alternating backgrounds
- Complete About page is server-rendered with zero new client components
- All 5 phase requirements (ABOUT-01 through ABOUT-05) are fulfilled
</success_criteria>

<output>
After completion, create `.planning/phases/04-about-page/04-02-SUMMARY.md`
</output>
