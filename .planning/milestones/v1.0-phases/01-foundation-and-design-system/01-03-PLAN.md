---
phase: 01-foundation-and-design-system
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified: []
autonomous: false
requirements:
  - FNDN-01
  - FNDN-10
must_haves:
  truths:
    - "All Phase 1 success criteria are visually confirmed by the project owner on the running dev server"
    - "The app is ready for Phase 2 (Layout Chrome) with all design tokens, typography, animation, and placeholder primitives verified"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 1 success criteria on the running dev server before proceeding to Phase 2.

Purpose: Human eyes confirm that the design system foundation looks correct and all primitives work as expected. Catching issues here (wrong font, missing color, broken animation) prevents compounding errors across all subsequent phases.

Output: Owner approval to proceed to Phase 2, or a list of issues to fix.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-design-system/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-design-system/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all Phase 1 design system primitives on dev server</name>
  <files>src/app/page.tsx</files>
  <action>
    This is a human verification checkpoint. No automated changes are made.

    What was built in Plans 01-01 and 01-02:
    1. Next.js 16+ project with TypeScript, App Router, and Tailwind CSS v4
    2. Brand color palette: forest green, cream, terracotta, charcoal, sage (plus light variants)
    3. Editorial typography: DM Serif Display (headings) + Plus Jakarta Sans (body)
    4. Framer Motion with LazyMotion + domAnimation for minimal bundle size
    5. AnimatedSection component for scroll-triggered fade-in/slide-up
    6. ImagePlaceholder component with descriptive labels and multiple aspect ratios
    7. GrainOverlay component for hero section noise texture
    8. Server-rendered pages with client components only for interactivity

    Present the verification checklist to the user and wait for their response.
  </action>
  <verify>
    Start the dev server if not already running: `npm run dev`

    Visit http://localhost:3000 and check each of the 5 Phase 1 Success Criteria:

    **1. Color Palette (FNDN-03)**
    - Page background should be warm cream (#f5f0e8), NOT white
    - Hero section should be deep forest green (#1a3a2a)
    - Scroll to "Brand Color Palette" section — all 7 color swatches should be distinct and match the brand

    **2. Typography (FNDN-04)**
    - "Yard Weasels Inc." hero heading should be in a classic serif font (DM Serif Display) — has visible serifs and editorial feel
    - Body text paragraphs should be in a clean, modern sans-serif (Plus Jakarta Sans) — NOT the browser default sans-serif
    - No flash of unstyled text on page load

    **3. Scroll Animations (FNDN-06)**
    - Scroll down slowly — each section should fade in and slide up as it enters the viewport
    - In the "Scroll Animations" section, the 3 green service cards should animate with visible staggered timing (left first, then middle, then right)
    - Animations should play only once (scrolling back up and down should not replay them)

    **4. Image Placeholders (FNDN-07)**
    - In the "Image Placeholders" section, 4 colored boxes with descriptive text labels should be visible
    - They should have different proportions (wide, square, tall)
    - They should look intentionally styled, not broken

    **5. Server Rendering (FNDN-10)**
    - Right-click the page and select "View Page Source" (or press Ctrl+U / Cmd+U)
    - The HTML source should contain the actual page content (headings, paragraphs, color labels) — NOT an empty shell with only script tags
  </verify>
  <done>Owner types "approved" to confirm all Phase 1 primitives are working correctly, or describes issues to fix.</done>
</task>

</tasks>

<verification>
All 5 Phase 1 success criteria confirmed by human inspection:
1. Brand colors render correctly (cream bg, forest heading, terracotta section, all swatches)
2. Typography: DM Serif Display on headings, Plus Jakarta Sans on body, no CLS
3. AnimatedSection triggers fade-in/slide-up on scroll with staggered delays
4. ImagePlaceholder shows styled boxes with labels in multiple aspect ratios
5. Page source contains full server-rendered HTML
</verification>

<success_criteria>
- Owner confirms "approved" after visual inspection of all Phase 1 primitives
- No blocking issues identified that would affect Phase 2 development
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-design-system/01-03-SUMMARY.md`
</output>
