---
phase: 08-contact-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/contact/ContactHero.tsx
  - src/components/sections/contact/ContactInfoPanel.tsx
  - src/components/sections/contact/ContactCallout.tsx
autonomous: true
requirements:
  - CONT-01
  - CONT-06
  - CONT-07
  - CONT-08

must_haves:
  truths:
    - "The Contact page has a hero with 'Get in Touch' headline and forest background"
    - "Both addresses (office + retail yard), phone, hours are displayed in the info panel"
    - "A styled map placeholder area is visible in the info panel"
    - "A 'Prefer to talk?' section shows the phone number 519-843-5489 as a clickable link"
  artifacts:
    - path: "src/components/sections/contact/ContactHero.tsx"
      provides: "Interior hero banner for contact page"
      min_lines: 15
    - path: "src/components/sections/contact/ContactInfoPanel.tsx"
      provides: "Contact info with addresses, phone, hours, and map placeholder"
      min_lines: 40
    - path: "src/components/sections/contact/ContactCallout.tsx"
      provides: "Prefer to talk CTA section with phone number"
      min_lines: 15
  key_links:
    - from: "src/components/sections/contact/ContactInfoPanel.tsx"
      to: "@/lib/data/navigation"
      via: "import companyInfo"
      pattern: "import.*companyInfo.*from.*navigation"
    - from: "src/components/sections/contact/ContactCallout.tsx"
      to: "@/lib/data/navigation"
      via: "import companyInfo"
      pattern: "import.*companyInfo.*from.*navigation"
---

<objective>
Build the Contact page server-side components: hero banner, contact info panel (addresses, phone, hours, map placeholder), and "Prefer to talk?" callout section.

Purpose: Establishes the page structure and all non-interactive sections so Plan 02 only needs to build the form component and assemble the final page.
Output: Three server components ready for page composition in Plan 02.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-contact-page/08-RESEARCH.md
@src/components/sections/about/AboutHero.tsx
@src/components/sections/services/ServicesContact.tsx
@src/lib/data/navigation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContactHero and ContactInfoPanel server components</name>
  <files>
    src/components/sections/contact/ContactHero.tsx
    src/components/sections/contact/ContactInfoPanel.tsx
  </files>
  <action>
Create two server components (NO 'use client'):

**ContactHero.tsx** — Copy the AboutHero pattern exactly:
- Forest background with `py-32 md:py-40 px-6 bg-forest overflow-hidden`
- Absolute-positioned ImagePlaceholder with label "Contact — Yard Weasels office and team"
- GrainOverlay
- Centered h1 "Get in Touch" in `font-display text-4xl md:text-5xl lg:text-6xl text-cream`
- Subtitle: "Ready to start your project? We'd love to hear from you." in `text-lg md:text-xl text-cream/80`

**ContactInfoPanel.tsx** — Server component displaying all contact info:
- Import `companyInfo` from `@/lib/data/navigation` (CRITICAL: do NOT hardcode addresses or phone)
- Import icons from lucide-react: MapPin, Phone, Clock, Mail
- Section heading "Contact Information" in `font-display text-2xl text-forest mb-8`
- Four info blocks, each with an icon + text:
  1. **Office Address** — MapPin icon, `companyInfo.officeAddress.label`, `.street`, `.city`
  2. **Retail Yard** — MapPin icon, `companyInfo.retailYardAddress.label`, `.street`, `.city`
  3. **Phone** — Phone icon, `companyInfo.phone` wrapped in `<a href={companyInfo.phoneHref}>` link
  4. **Hours** — Clock icon, `companyInfo.hours`
- Space each block with `space-y-6` or similar
- Below the info blocks, add a **map placeholder** (CONT-07):
  - A `div` with `rounded-lg overflow-hidden bg-sage/20 aspect-[4/3]` (landscape rectangle)
  - Inside: centered MapPin icon (larger, e.g. `w-8 h-8 text-forest/50`), the office address text, and a subtle "Interactive map coming soon" label
  - Style: sage-tinted background, rounded corners, clearly reads as "map goes here"
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Visually confirm both files exist and have no 'use client' directive.
  </verify>
  <done>
ContactHero renders a forest-bg hero with "Get in Touch" headline. ContactInfoPanel displays both addresses, phone, hours from companyInfo (not hardcoded), and a styled map placeholder area. Both are server components.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ContactCallout CTA section</name>
  <files>
    src/components/sections/contact/ContactCallout.tsx
  </files>
  <action>
Create a server component (NO 'use client') following the ServicesContact pattern:

- Import AnimatedSection, GrainOverlay, and `companyInfo` from navigation.ts
- Forest background: `relative py-20 md:py-28 px-6 bg-forest overflow-hidden`
- GrainOverlay for texture
- AnimatedSection wrapper with `relative z-10`
- Centered content with `max-w-3xl mx-auto text-center`:
  - h2: "Prefer to Talk?" in `font-display text-3xl md:text-4xl text-cream mb-4`
  - Paragraph: "Call us directly at" followed by a clickable phone link using `companyInfo.phone` and `companyInfo.phoneHref`
  - Phone link styled: `text-cream underline font-medium hover:text-cream/80 transition-colors`
  - Additional text below: "We're available {companyInfo.hours}" in `text-cream/70`

This directly satisfies CONT-08: "Prefer to talk? Call us directly at 519-843-5489"
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Confirm file has no 'use client' directive. Check phone number comes from companyInfo import.
  </verify>
  <done>
ContactCallout renders a forest-bg CTA section with "Prefer to Talk?" headline and clickable phone number from companyInfo. It is a server component using the same pattern as ServicesContact.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. All three components exist in `src/components/sections/contact/`
3. None of the three files contain `'use client'`
4. ContactInfoPanel imports `companyInfo` from `@/lib/data/navigation` — no hardcoded addresses
5. ContactCallout imports `companyInfo` — phone number is not hardcoded
6. Map placeholder area is visible in ContactInfoPanel with sage/forest styling
</verification>

<success_criteria>
Three server components ready for page assembly: ContactHero (hero banner), ContactInfoPanel (addresses, phone, hours, map placeholder), and ContactCallout ("Prefer to talk?" CTA). All use canonical data from navigation.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/08-contact-page/08-01-SUMMARY.md`
</output>
