---
phase: 03-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/services.ts
  - src/lib/data/products.ts
  - src/lib/data/testimonials.ts
  - src/lib/data/projects.ts
  - src/components/sections/HeroParallax.tsx
  - src/components/sections/HeroSection.tsx
  - src/components/sections/ScrollIndicator.tsx
  - src/app/page.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04]

must_haves:
  truths:
    - "The hero fills the full viewport with a parallax background and grain texture overlay"
    - "The headline 'Crafting Outdoor Spaces That Inspire' displays in bold serif with Fergus Ontario subline"
    - "Two CTA buttons are visible: terracotta 'Get a Free Quote' and outlined 'View Our Work'"
    - "A bouncing scroll-down chevron indicator animates at the bottom of the hero"
  artifacts:
    - path: "src/components/sections/HeroParallax.tsx"
      provides: "Parallax wrapper using useScroll + useTransform"
      contains: "use client"
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero composition with headline, CTAs, grain overlay"
      contains: "Crafting Outdoor Spaces"
    - path: "src/components/sections/ScrollIndicator.tsx"
      provides: "Bouncing chevron-down scroll indicator"
      contains: "animate-bounce"
    - path: "src/lib/data/services.ts"
      provides: "Service card data for Residential, Commercial, Municipal"
    - path: "src/lib/data/products.ts"
      provides: "7 product category entries"
    - path: "src/lib/data/testimonials.ts"
      provides: "3 placeholder testimonials with ratings"
    - path: "src/lib/data/projects.ts"
      provides: "4-6 showcase project entries"
    - path: "src/app/page.tsx"
      provides: "Server Component homepage assembling hero section"
  key_links:
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/components/sections/HeroParallax.tsx"
      via: "import HeroParallax"
      pattern: "HeroParallax"
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/components/ui/GrainOverlay.tsx"
      via: "import GrainOverlay"
      pattern: "GrainOverlay"
    - from: "src/app/page.tsx"
      to: "src/components/sections/HeroSection.tsx"
      via: "import HeroSection"
      pattern: "HeroSection"
---

<objective>
Create the full-viewport hero section with parallax background, grain texture, headline, CTAs, and scroll indicator, plus all static data files needed by subsequent homepage plans.

Purpose: The hero is the first thing visitors see — it must immediately communicate premium quality and establish the visual language. Data files are created here so Plans 02 and 03 can focus purely on section components.
Output: HeroParallax (client), HeroSection (server), ScrollIndicator (server), 4 data files, and page.tsx scaffolded with hero.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/03-RESEARCH.md
@.planning/phases/02-layout-chrome/02-01-SUMMARY.md
@.planning/phases/02-layout-chrome/02-02-SUMMARY.md
@src/components/ui/AnimatedSection.tsx
@src/components/ui/GrainOverlay.tsx
@src/components/ui/ImagePlaceholder.tsx
@src/lib/data/navigation.ts
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static data files for services, products, testimonials, and projects</name>
  <files>
    src/lib/data/services.ts
    src/lib/data/products.ts
    src/lib/data/testimonials.ts
    src/lib/data/projects.ts
  </files>
  <action>
Create 4 typed data files in `src/lib/data/`:

**services.ts** — Export a `services` array of 3 objects (Residential Landscaping, Commercial Landscaping, Municipal Projects). Each has: `id` (string slug), `title` (string), `description` (string, 1-2 sentences about the service), `image` (string label for ImagePlaceholder), `href` (string link to /services). Export the `Service` interface.

**products.ts** — Export a `products` array of 7 objects for: Aggregates, Mulch, Fertilizer, Natural Stone, Topsoil, Interlock, Seed. Each has: `id` (string slug), `name` (string), `description` (string, short tagline), `image` (string label for ImagePlaceholder). Export the `Product` interface.

**testimonials.ts** — Export a `testimonials` array of 3 placeholder objects. Each has: `id` (number), `quote` (string, 2-3 sentences of realistic-sounding landscaping praise), `author` (string, placeholder name), `role` (string, e.g. "Homeowner, Fergus"), `rating` (number, 5 for all). Export the `Testimonial` interface.

**projects.ts** — Export a `projects` array of 6 objects. Each has: `id` (string slug), `name` (string, descriptive project name like "Riverside Patio & Pergola"), `category` (string, one of: Residential, Commercial, Hardscaping, Irrigation), `image` (string label for ImagePlaceholder), `featured` (boolean, true for 2 items). Export the `Project` interface.

Use `as const` satisfies pattern or simple typed arrays. All data is static — no runtime dependencies.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm all 4 files have valid TypeScript with properly exported interfaces and data arrays. Import each in a test: `import { services } from '@/lib/data/services'` etc.
  </verify>
  <done>4 typed data files exist with realistic placeholder content. Each exports a named array and a TypeScript interface. Zero runtime dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Build HeroParallax, ScrollIndicator, HeroSection, and wire into page.tsx</name>
  <files>
    src/components/sections/HeroParallax.tsx
    src/components/sections/HeroSection.tsx
    src/components/sections/ScrollIndicator.tsx
    src/app/page.tsx
  </files>
  <action>
Create the `src/components/sections/` directory and build 3 components:

**HeroParallax.tsx** (`'use client'`):
- Import `useRef` from React, `{ m, useScroll, useTransform }` from `motion/react`
- Accept `children: React.ReactNode` prop
- Create a ref for the section element
- Use `useScroll({ target: ref, offset: ['start start', 'end start'] })` to get `scrollYProgress`
- Use `useTransform(scrollYProgress, [0, 1], ['0%', '50%'])` for the parallax Y value
- Render: `<section ref={ref} className="relative min-h-screen overflow-hidden">` containing:
  - `<m.div className="absolute inset-0 -z-10" style={{ y }}>` wrapping an ImagePlaceholder with `label="Hero — Completed landscape project"`, `className="absolute inset-0 w-full h-full !rounded-none"`, and appropriate styling to fill the container (the ImagePlaceholder needs a wrapper div with `className="h-full w-full"` and the placeholder itself uses `className="!aspect-auto h-full w-full !rounded-none"`)
  - `<div className="relative z-20">{children}</div>` for foreground content
- Respect `prefers-reduced-motion`: use `useReducedMotion()` from `motion/react`. If reduced motion preferred, set parallax Y to `'0%'` (no movement).

**ScrollIndicator.tsx** (Server Component, NO `'use client'`):
- Import `ChevronDown` from `lucide-react`
- Render an absolutely positioned div at bottom of hero: `className="absolute bottom-8 left-1/2 -translate-x-1/2 z-20"`
- Inside: `<div className="animate-bounce">` wrapping `<ChevronDown className="h-8 w-8 text-cream/60" />`
- Add `aria-hidden="true"` since it's decorative

**HeroSection.tsx** (Server Component, NO `'use client'`):
- Import HeroParallax, ScrollIndicator, GrainOverlay, and Link from next/link
- Compose the hero: HeroParallax wrapping:
  - GrainOverlay component
  - Content div with `flex items-center justify-center min-h-screen` and padding
  - Inside content: centered text container with:
    - `<h1 className="font-display text-5xl md:text-6xl lg:text-7xl text-cream mb-6 max-w-4xl">Crafting Outdoor Spaces That Inspire</h1>`
    - `<p className="text-lg md:text-xl text-cream/80 mb-10 max-w-2xl">` with subline mentioning Fergus, Ontario (e.g., "Premium landscaping design, build, and maintenance serving Fergus, Ontario and surrounding communities")
    - Two CTA buttons in a flex row with gap-4:
      1. Link to `/contact`: `className="inline-flex items-center px-8 py-4 bg-terracotta hover:bg-terracotta-light text-cream font-medium rounded-lg transition-colors"` with text "Get a Free Quote"
      2. Link to `/gallery`: `className="inline-flex items-center px-8 py-4 border-2 border-cream text-cream hover:bg-cream/10 font-medium rounded-lg transition-colors"` with text "View Our Work"
  - ScrollIndicator positioned at the bottom of the parallax section

**page.tsx** — Replace the entire current test/demo page with:
- Server Component (NO `'use client'`)
- Import and render HeroSection
- Wrap everything in a `<div>` (not `<main>`, per established pattern — root layout owns `<main>`)
- Add comment placeholders for sections that Plans 02 and 03 will add: `{/* Services Preview — Plan 03-02 */}` etc.
  </action>
  <verify>
1. Run `npx tsc --noEmit` — zero TypeScript errors
2. Run `npm run build` — production build succeeds
3. Verify page.tsx has NO `'use client'` directive
4. Verify only HeroParallax.tsx has `'use client'`
5. Check that `src/components/sections/` directory contains HeroParallax.tsx, HeroSection.tsx, ScrollIndicator.tsx
  </verify>
  <done>Full-viewport hero renders with parallax background placeholder, grain texture overlay, "Crafting Outdoor Spaces That Inspire" headline in serif, Fergus Ontario subline, two styled CTA buttons, and a bouncing scroll indicator. Page is server-rendered.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `npx tsc --noEmit` passes — all types valid
3. `grep -r "'use client'" src/components/sections/` shows ONLY HeroParallax.tsx
4. `grep "'use client'" src/app/page.tsx` returns nothing (page is Server Component)
5. All 4 data files exist and export typed arrays
6. Dev server shows full-viewport hero with headline, CTAs, and scroll indicator
</verification>

<success_criteria>
- Hero section fills the entire viewport with a parallax-capable background and grain overlay
- "Crafting Outdoor Spaces That Inspire" headline renders in DM Serif Display (bold serif)
- Fergus Ontario subline is visible below the headline
- "Get a Free Quote" button is terracotta, "View Our Work" button is outlined
- Scroll indicator bounces at the bottom of the hero
- 4 data files provide typed content for all subsequent homepage sections
- Page remains server-rendered (no 'use client' on page.tsx)
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-01-SUMMARY.md`
</output>
