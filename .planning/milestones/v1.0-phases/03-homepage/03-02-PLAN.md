---
phase: 03-homepage
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/sections/ServicesPreview.tsx
  - src/components/sections/AboutTeaser.tsx
  - src/components/sections/ProductsBanner.tsx
  - src/app/page.tsx
autonomous: true
requirements: [HOME-05, HOME-06, HOME-07, HOME-11]

must_haves:
  truths:
    - "Three service cards (Residential, Commercial, Municipal) display in a 3-column grid with image placeholders and hover lift effect"
    - "The about teaser shows a split layout with image placeholder left and 'Rooted in Fergus' headline right"
    - "A products banner displays all 7 product categories with the retail yard address"
    - "All three sections have staggered scroll-triggered fade-in/slide-up entrance animations"
  artifacts:
    - path: "src/components/sections/ServicesPreview.tsx"
      provides: "3-column service card grid with hover lift"
      contains: "services"
    - path: "src/components/sections/AboutTeaser.tsx"
      provides: "Split layout about teaser with image and text"
      contains: "Rooted in Fergus"
    - path: "src/components/sections/ProductsBanner.tsx"
      provides: "7 product categories grid with retail yard address"
      contains: "products"
    - path: "src/app/page.tsx"
      provides: "Homepage with hero + services + about + products sections"
  key_links:
    - from: "src/components/sections/ServicesPreview.tsx"
      to: "src/lib/data/services.ts"
      via: "import { services }"
      pattern: "services"
    - from: "src/components/sections/ProductsBanner.tsx"
      to: "src/lib/data/products.ts"
      via: "import { products }"
      pattern: "products"
    - from: "src/components/sections/ProductsBanner.tsx"
      to: "src/lib/data/navigation.ts"
      via: "import { companyInfo }"
      pattern: "companyInfo.retailYardAddress"
    - from: "src/app/page.tsx"
      to: "src/components/sections/ServicesPreview.tsx"
      via: "import ServicesPreview"
      pattern: "ServicesPreview"
---

<objective>
Build the three content sections between the hero and the project showcase: services preview cards, about teaser, and products banner — all with scroll-triggered animations.

Purpose: These sections establish what YWI does (services), who they are (about), and what they sell (products), filling out the homepage content between the hero and the social proof sections.
Output: ServicesPreview, AboutTeaser, ProductsBanner components integrated into page.tsx.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/03-RESEARCH.md
@.planning/phases/03-homepage/03-01-SUMMARY.md
@src/components/ui/AnimatedSection.tsx
@src/components/ui/ImagePlaceholder.tsx
@src/lib/data/services.ts
@src/lib/data/products.ts
@src/lib/data/navigation.ts
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServicesPreview, AboutTeaser, and ProductsBanner section components</name>
  <files>
    src/components/sections/ServicesPreview.tsx
    src/components/sections/AboutTeaser.tsx
    src/components/sections/ProductsBanner.tsx
  </files>
  <action>
All three components are Server Components (NO `'use client'`). They use AnimatedSection for scroll animations and ImagePlaceholder for image slots.

**ServicesPreview.tsx** (Server Component):
- Import `services` from `@/lib/data/services`, AnimatedSection, ImagePlaceholder, Link from `next/link`
- Section with `py-20 md:py-28 px-6 bg-white` styling
- Wrap the entire section in AnimatedSection
- Centered `max-w-7xl mx-auto` container
- Section header: `<h2 className="font-display text-3xl md:text-4xl text-forest text-center mb-4">` with "Our Services"
- Subtitle paragraph: `<p className="text-charcoal/70 text-center max-w-2xl mx-auto mb-12">` with text like "From design and build to year-round maintenance, we bring craftsmanship to every project."
- `<div className="grid md:grid-cols-3 gap-8">` containing 3 cards mapped from `services` array
- Each card wrapped in `<AnimatedSection delay={index * 0.15}>` for staggered animation
- Card structure: `<Link href={service.href} className="group rounded-lg overflow-hidden bg-white shadow-sm hover:shadow-lg hover:-translate-y-2 transition-all duration-300">`
  - ImagePlaceholder with `label={service.image}` and `aspectRatio="video"`
  - Content div with `p-6`: service title in `font-display text-xl text-forest mb-2`, description in `text-charcoal/70 text-sm leading-relaxed`, and "Learn More →" span with `text-terracotta font-medium text-sm group-hover:translate-x-1 transition-transform inline-block mt-4`

**AboutTeaser.tsx** (Server Component):
- Import AnimatedSection, ImagePlaceholder, Link from `next/link`
- Section with `py-20 md:py-28 px-6` styling (no bg-white, uses default cream/light background)
- Wrap in AnimatedSection
- Centered `max-w-7xl mx-auto` container
- `<div className="grid md:grid-cols-2 gap-12 items-center">` for split layout
- Left side: `<AnimatedSection delay={0}>` wrapping ImagePlaceholder with `label="YWI team on a residential project site"` and `aspectRatio="square"` and additional `className="rounded-xl"`
- Right side: `<AnimatedSection delay={0.2}>` wrapping:
  - `<h2 className="font-display text-3xl md:text-4xl text-forest mb-6">Rooted in Fergus</h2>`
  - Two paragraphs of intro text in `text-charcoal/70 leading-relaxed mb-4` describing YWI's community roots, range of clients (residential, commercial, municipal), and commitment to quality
  - Link to `/about` styled as: `className="inline-flex items-center text-terracotta font-medium hover:text-terracotta-light transition-colors"` with text "Our Story →"

**ProductsBanner.tsx** (Server Component):
- Import `products` from `@/lib/data/products`, `companyInfo` from `@/lib/data/navigation`, AnimatedSection, ImagePlaceholder
- Section with `py-20 md:py-28 px-6 bg-forest relative overflow-hidden` styling
- Import GrainOverlay from `@/components/ui/GrainOverlay` for subtle texture on the dark background
- Wrap in AnimatedSection
- Centered `max-w-7xl mx-auto relative z-10` container
- Section header: `<h2 className="font-display text-3xl md:text-4xl text-cream text-center mb-4">Quality Materials</h2>`
- Subtitle: `<p className="text-cream/70 text-center max-w-2xl mx-auto mb-12">` with text about their retail yard carrying premium landscape supplies
- Product grid: `<div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4">` mapping `products` array
- Each product in `<AnimatedSection delay={index * 0.1}>` wrapping a card:
  - Card: `<div className="group rounded-lg overflow-hidden bg-cream/10 backdrop-blur-sm hover:bg-cream/20 transition-colors">`
  - ImagePlaceholder with `label={product.image}` and `aspectRatio="square"` and `className="!bg-sage/20"`
  - Name in `<p className="p-3 text-center text-cream font-medium text-sm">{product.name}</p>`
- Below the grid: retail yard address info in `<div className="text-center mt-12">`:
  - `<p className="text-cream/70 text-sm">Visit our retail yard</p>`
  - `<p className="text-cream font-medium">{companyInfo.retailYardAddress.street}, {companyInfo.retailYardAddress.city}</p>`
  </action>
  <verify>
1. Run `npx tsc --noEmit` — zero TypeScript errors
2. Verify NO `'use client'` in any of the 3 files: `grep "'use client'" src/components/sections/ServicesPreview.tsx src/components/sections/AboutTeaser.tsx src/components/sections/ProductsBanner.tsx` returns nothing
3. Verify imports from data files: `grep "from.*data/" src/components/sections/ServicesPreview.tsx src/components/sections/ProductsBanner.tsx`
  </verify>
  <done>Three section components exist as Server Components, each using AnimatedSection for scroll animations. ServicesPreview shows 3 cards with hover lift, AboutTeaser has split layout with "Rooted in Fergus", ProductsBanner shows 7 products in a grid with retail yard address.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ServicesPreview, AboutTeaser, and ProductsBanner into page.tsx</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
Update `src/app/page.tsx` to add the 3 new section imports and render them below the HeroSection:

1. Add imports at top: `import ServicesPreview from '@/components/sections/ServicesPreview'`, `import AboutTeaser from '@/components/sections/AboutTeaser'`, `import ProductsBanner from '@/components/sections/ProductsBanner'`
2. After `<HeroSection />`, add in order:
   - `<ServicesPreview />`
   - `<AboutTeaser />`
   - `<ProductsBanner />`
3. Keep the comment placeholders for Plan 03-03 sections: `{/* Project Showcase — Plan 03-03 */}`, `{/* Testimonial Carousel — Plan 03-03 */}`, `{/* CTA Banner — Plan 03-03 */}`
4. Do NOT add `'use client'` to page.tsx — it remains a Server Component
  </action>
  <verify>
1. Run `npm run build` — production build succeeds
2. `grep "'use client'" src/app/page.tsx` returns nothing
3. Dev server shows hero followed by services cards, about teaser, and products banner with scroll animations
  </verify>
  <done>page.tsx renders HeroSection, ServicesPreview, AboutTeaser, and ProductsBanner in order. All sections have visible staggered scroll animations. Page is still server-rendered.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `npx tsc --noEmit` passes
3. `grep -r "'use client'" src/components/sections/` shows ONLY HeroParallax.tsx (from Plan 01)
4. Dev server shows services cards with hover lift, about split layout, and products grid with retail address
5. All sections animate in on scroll with staggered timing
</verification>

<success_criteria>
- 3-column service card layout shows Residential, Commercial, Municipal with hover lift effect and image placeholders
- About teaser has split layout: image placeholder left, "Rooted in Fergus" headline and intro text right
- Products banner shows all 7 product categories in a responsive grid on forest green background
- Retail yard address (6470 Beatty Line N) appears below the products grid
- All sections have staggered scroll-triggered fade-in/slide-up animations via AnimatedSection
- Page remains server-rendered (no 'use client' on page.tsx)
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-02-SUMMARY.md`
</output>
