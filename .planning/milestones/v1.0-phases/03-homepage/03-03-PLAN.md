---
phase: 03-homepage
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/components/sections/ProjectShowcase.tsx
  - src/components/sections/TestimonialCarousel.tsx
  - src/components/sections/CTABanner.tsx
  - src/app/page.tsx
autonomous: true
requirements: [HOME-08, HOME-09, HOME-10, HOME-11]

must_haves:
  truths:
    - "A project showcase displays 4-6 image placeholders in an asymmetric grid with hover overlays showing project names"
    - "A testimonial carousel auto-advances through 3 testimonials with star ratings, quotation marks, and smooth transitions"
    - "A full-width CTA banner shows 'Ready to Transform Your Outdoor Space?' with a quote button and phone number on a textured background"
    - "All sections have staggered scroll-triggered entrance animations"
    - "The entire homepage is server-rendered — page source contains full HTML"
  artifacts:
    - path: "src/components/sections/ProjectShowcase.tsx"
      provides: "Asymmetric CSS Grid project gallery with hover overlays"
      contains: "projects"
    - path: "src/components/sections/TestimonialCarousel.tsx"
      provides: "Auto-advancing carousel with AnimatePresence transitions"
      contains: "use client"
    - path: "src/components/sections/CTABanner.tsx"
      provides: "Full-width CTA with textured background, headline, quote button, phone"
      contains: "Ready to Transform"
    - path: "src/app/page.tsx"
      provides: "Complete homepage with all 7 sections"
  key_links:
    - from: "src/components/sections/ProjectShowcase.tsx"
      to: "src/lib/data/projects.ts"
      via: "import { projects }"
      pattern: "projects"
    - from: "src/components/sections/TestimonialCarousel.tsx"
      to: "testimonials prop"
      via: "props from page.tsx"
      pattern: "testimonials"
    - from: "src/components/sections/CTABanner.tsx"
      to: "src/lib/data/navigation.ts"
      via: "import { companyInfo }"
      pattern: "companyInfo.phone"
    - from: "src/app/page.tsx"
      to: "all 7 section components"
      via: "imports"
      pattern: "import.*from.*sections/"
---

<objective>
Build the project showcase, testimonial carousel, and CTA banner — completing all homepage sections and finalizing page.tsx with all 7 sections assembled.

Purpose: These final sections provide social proof (projects, testimonials) and a clear conversion path (CTA banner), completing the premium homepage experience.
Output: ProjectShowcase (server), TestimonialCarousel (client), CTABanner (server) integrated into the complete page.tsx.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/03-RESEARCH.md
@.planning/phases/03-homepage/03-01-SUMMARY.md
@.planning/phases/03-homepage/03-02-SUMMARY.md
@src/components/ui/AnimatedSection.tsx
@src/components/ui/ImagePlaceholder.tsx
@src/components/ui/GrainOverlay.tsx
@src/lib/data/projects.ts
@src/lib/data/testimonials.ts
@src/lib/data/navigation.ts
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProjectShowcase, TestimonialCarousel, and CTABanner section components</name>
  <files>
    src/components/sections/ProjectShowcase.tsx
    src/components/sections/TestimonialCarousel.tsx
    src/components/sections/CTABanner.tsx
  </files>
  <action>
**ProjectShowcase.tsx** (Server Component, NO `'use client'`):
- Import `projects` from `@/lib/data/projects`, AnimatedSection, ImagePlaceholder, Link from `next/link`
- Section with `py-20 md:py-28 px-6 bg-white` styling
- Wrap in AnimatedSection
- Centered `max-w-7xl mx-auto` container
- Header: `<h2 className="font-display text-3xl md:text-4xl text-forest text-center mb-4">Our Work</h2>`
- Subtitle: `<p className="text-charcoal/70 text-center max-w-2xl mx-auto mb-12">` about showcasing recent projects
- Asymmetric CSS Grid: `<div className="grid grid-cols-2 md:grid-cols-3 gap-4">`
- Map `projects` array. For items where `project.featured` is true, add `col-span-2 row-span-2` classes. All items get:
  - Wrapper: `<AnimatedSection delay={index * 0.1}>` wrapping `<div className="group relative overflow-hidden rounded-lg">`
  - ImagePlaceholder with `label={project.image}` and `aspectRatio="square"`
  - Hover overlay: `<div className="absolute inset-0 bg-charcoal/60 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end p-4 md:p-6">`
    - Inside overlay: `<div>` with project name in `<span className="text-cream font-display text-lg md:text-xl">{project.name}</span>` and category in `<span className="text-cream/70 text-sm block">{project.category}</span>`
- Below grid: Link to `/gallery` styled as centered text link: `<div className="text-center mt-10"><Link href="/gallery" className="inline-flex items-center text-terracotta font-medium hover:text-terracotta-light transition-colors">View All Projects →</Link></div>`

**TestimonialCarousel.tsx** (`'use client'`):
- Import `useState`, `useEffect`, `useCallback` from React
- Import `AnimatePresence`, `m` from `motion/react`
- Accept `testimonials` prop typed as array of `{ id: number; quote: string; author: string; role: string; rating: number }`
- State: `current` (number, default 0), `isPaused` (boolean, default false)
- `next` callback: `setCurrent(prev => (prev + 1) % testimonials.length)`
- `prev` callback: `setCurrent(prev => (prev - 1 + testimonials.length) % testimonials.length)`
- Auto-advance with `useEffect`: if not paused, `setTimeout(next, 5000)`. Dependencies: `[current, isPaused, next]`. Clean up timer on unmount.
- Inline `StarRating` component: renders 5 inline SVG stars (filled = `text-terracotta`, empty = `text-charcoal/20`). Include `aria-label={'{rating} out of 5 stars'}` on the container div.
- Section layout: `<section className="py-20 md:py-28 px-6">` (this component manages its own section tag since it's a client component)
  - Centered `max-w-4xl mx-auto text-center` container
  - Section title: `<h2 className="font-display text-3xl md:text-4xl text-forest mb-16">What Our Clients Say</h2>`
  - Carousel area with `onMouseEnter={() => setIsPaused(true)}` and `onMouseLeave={() => setIsPaused(false)}`:
    - Decorative quotation mark: `<span className="font-display text-7xl md:text-8xl text-terracotta/20 leading-none select-none block mb-4" aria-hidden="true">{'\u201C'}</span>`
    - `<AnimatePresence mode="wait">` wrapping `<m.div key={current} initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -20 }} transition={{ duration: 0.4, ease: [0.22, 1, 0.36, 1] }}>`
      - Quote text: `<blockquote className="text-lg md:text-xl text-charcoal leading-relaxed mb-6 max-w-2xl mx-auto">{testimonial.quote}</blockquote>`
      - StarRating component with rating
      - Author info: `<p className="font-display text-forest mt-4">{testimonial.author}</p>` and `<p className="text-charcoal/60 text-sm">{testimonial.role}</p>`
    - Dot indicators below carousel: `<div className="flex justify-center gap-3 mt-8">` mapping testimonials to buttons:
      - `<button onClick={() => { setCurrent(i); setIsPaused(false) }} className={cn('w-3 h-3 rounded-full transition-colors', current === i ? 'bg-terracotta' : 'bg-charcoal/20 hover:bg-charcoal/40')} aria-label={'Go to testimonial ' + (i + 1)} />`
  - Import `cn` from `@/lib/utils/cn` for dot indicator conditional styling

**CTABanner.tsx** (Server Component, NO `'use client'`):
- Import `companyInfo` from `@/lib/data/navigation`, GrainOverlay, Link from `next/link`
- Section: `<section className="relative py-20 md:py-28 px-6 bg-terracotta overflow-hidden">`
- GrainOverlay for texture
- AnimatedSection wrapping content at `relative z-10`
- Centered `max-w-4xl mx-auto text-center` container:
  - `<h2 className="font-display text-3xl md:text-4xl lg:text-5xl text-cream mb-6">Ready to Transform Your Outdoor Space?</h2>`
  - `<p className="text-cream/80 text-lg mb-10 max-w-2xl mx-auto">` with a line like "Let's discuss your vision. Get a free consultation and quote for your project."
  - CTA row: `<div className="flex flex-col sm:flex-row items-center justify-center gap-4">`
    - Link to `/contact`: `className="inline-flex items-center px-8 py-4 bg-forest hover:bg-forest-light text-cream font-medium rounded-lg transition-colors"` with text "Get a Free Quote"
    - Phone link: `<a href={companyInfo.phoneHref} className="inline-flex items-center px-8 py-4 border-2 border-cream text-cream hover:bg-cream/10 font-medium rounded-lg transition-colors">` with text `Call {companyInfo.phone}`
  </action>
  <verify>
1. Run `npx tsc --noEmit` — zero TypeScript errors
2. `grep "'use client'" src/components/sections/TestimonialCarousel.tsx` confirms it's a client component
3. `grep "'use client'" src/components/sections/ProjectShowcase.tsx src/components/sections/CTABanner.tsx` returns nothing (both Server Components)
4. Verify TestimonialCarousel accepts testimonials as prop (not importing data directly — data flows from server page.tsx)
  </verify>
  <done>ProjectShowcase shows asymmetric grid with hover overlays. TestimonialCarousel auto-advances with star ratings, quotation marks, and dot indicators. CTABanner has textured background with headline, quote button, and phone number.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize page.tsx with all 7 homepage sections</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
Update `src/app/page.tsx` to import and render the final 3 sections, completing the homepage:

1. Add imports: `import ProjectShowcase from '@/components/sections/ProjectShowcase'`, `import TestimonialCarousel from '@/components/sections/TestimonialCarousel'`, `import CTABanner from '@/components/sections/CTABanner'`, `import { testimonials } from '@/lib/data/testimonials'`
2. After ProductsBanner, add in order:
   - `<ProjectShowcase />`
   - `<TestimonialCarousel testimonials={testimonials} />` — note: testimonials data is imported in page.tsx (Server Component) and passed as prop to the client component. This keeps the data server-side.
   - `<CTABanner />`
3. Remove any remaining placeholder comments from Plan 01/02
4. Do NOT add `'use client'` to page.tsx
5. Final page.tsx should render exactly: HeroSection, ServicesPreview, AboutTeaser, ProductsBanner, ProjectShowcase, TestimonialCarousel, CTABanner — in that order, wrapped in a single `<div>`
  </action>
  <verify>
1. Run `npm run build` — production build succeeds with zero errors
2. `grep "'use client'" src/app/page.tsx` returns nothing (Server Component confirmed)
3. `curl http://localhost:3000 2>/dev/null | grep -c "Crafting Outdoor Spaces"` returns 1 (SSR confirmed — full HTML in page source)
4. `curl http://localhost:3000 2>/dev/null | grep -c "Ready to Transform"` returns 1 (CTA banner in SSR output)
5. Dev server shows complete homepage: hero → services → about → products → projects → testimonials → CTA
  </verify>
  <done>page.tsx renders all 7 homepage sections. The page is fully server-rendered. Testimonial data flows from server to client component via props. The complete homepage is assembled and functional.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `npx tsc --noEmit` passes
3. `grep -r "'use client'" src/components/sections/` shows ONLY HeroParallax.tsx and TestimonialCarousel.tsx
4. `grep "'use client'" src/app/page.tsx` returns nothing
5. Dev server shows all 7 sections in order with scroll animations
6. Testimonial carousel auto-advances every 5 seconds, pauses on hover, has dot navigation
7. Project showcase has hover overlays revealing project names
8. CTA banner shows phone number and quote button on textured terracotta background
9. `curl http://localhost:3000` returns full HTML including section content (SSR verified)
</verification>

<success_criteria>
- Project showcase displays 6 projects in asymmetric grid with hover overlays showing name and category
- Testimonial carousel auto-advances through 3 testimonials with star ratings and quotation mark decoration
- Hovering the carousel pauses auto-advance; clicking dots navigates manually
- CTA banner fills full width with terracotta + grain texture, "Ready to Transform Your Outdoor Space?" headline, quote button and phone number
- All sections have staggered scroll-triggered fade-in/slide-up animations
- The entire page is server-rendered — curl returns complete HTML with all section content
- Only HeroParallax.tsx and TestimonialCarousel.tsx have 'use client' in the entire sections/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-03-SUMMARY.md`
</output>
