---
phase: 09-seo-and-performance
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/components/sections/HeroSection.tsx
  - src/components/sections/HeroParallax.tsx
  - src/components/sections/about/AboutHero.tsx
  - src/components/sections/services/ServicesHero.tsx
  - src/components/sections/products/ProductsHero.tsx
  - src/components/sections/gallery/GalleryHero.tsx
  - src/components/sections/contact/ContactHero.tsx
  - src/components/ui/ImagePlaceholder.tsx
autonomous: false
requirements:
  - SEO-04
  - SEO-05
  - SEO-06
  - SEO-07
  - SEO-08

must_haves:
  truths:
    - "Visiting /sitemap.xml returns valid XML listing all six pages with canonical URLs"
    - "Visiting /robots.txt returns valid robots file allowing all crawlers and referencing sitemap"
    - "Hero components pass priority={true} to ImagePlaceholder, marking LCP images for next/image migration"
    - "Every ImagePlaceholder call site has a non-empty label prop (future alt text)"
    - "ImagePlaceholder documents the migration path from label prop to alt text + sizes"
    - "npm run build succeeds and production build is clean"
    - "Lighthouse performance score 90+ verified on deployed preview URL"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Sitemap route handler for all 6 pages"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "Robots.txt route handler"
      exports: ["default"]
  key_links:
    - from: "src/app/sitemap.ts"
      to: "src/lib/metadata.ts"
      via: "import siteConfig for base URL"
      pattern: "import.*siteConfig.*from.*metadata"
    - from: "src/app/robots.ts"
      to: "src/lib/metadata.ts"
      via: "import siteConfig for sitemap URL"
      pattern: "import.*siteConfig.*from.*metadata"
---

<objective>
Add sitemap.ts and robots.ts route handlers for crawler access, document the hero image priority migration path for LCP optimization, and verify the production build achieves Lighthouse performance targets.

Purpose: Ensure search engines can discover and crawl all pages via sitemap/robots, prepare the image optimization infrastructure for when real photos replace placeholders, and confirm overall performance health.
Output: `src/app/sitemap.ts`, `src/app/robots.ts`, priority/sizes comments in hero components, and a verified production build.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-seo-and-performance/09-RESEARCH.md
@.planning/phases/09-seo-and-performance/09-01-SUMMARY.md
@src/lib/metadata.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sitemap.ts and robots.ts route handlers</name>
  <files>
    src/app/sitemap.ts
    src/app/robots.ts
  </files>
  <action>
    Create `src/app/sitemap.ts`:
    - Import `MetadataRoute` type from `next`
    - Import `siteConfig` from `@/lib/metadata` to get the base URL
    - Export default function `sitemap()` returning `MetadataRoute.Sitemap`
    - List all 6 pages with their URLs, `lastModified: new Date()`, `changeFrequency`, and `priority`:
      - `/` — changeFrequency: 'monthly', priority: 1
      - `/about` — changeFrequency: 'monthly', priority: 0.8
      - `/services` — changeFrequency: 'monthly', priority: 0.9
      - `/products` — changeFrequency: 'monthly', priority: 0.8
      - `/gallery` — changeFrequency: 'weekly', priority: 0.7
      - `/contact` — changeFrequency: 'yearly', priority: 0.8
    - Use `siteConfig.url` as the base for all URL construction

    Create `src/app/robots.ts`:
    - Import `MetadataRoute` type from `next`
    - Import `siteConfig` from `@/lib/metadata` to get the sitemap URL
    - Export default function `robots()` returning `MetadataRoute.Robots`
    - Rules: `userAgent: '*'`, `allow: '/'`
    - Sitemap: `${siteConfig.url}/sitemap.xml`
  </action>
  <verify>
    Run `npm run build` to confirm sitemap.ts and robots.ts compile. Run `npm run start` (or `npx next start`) in background, then:
    - `curl http://localhost:3000/sitemap.xml` — verify valid XML with 6 URL entries
    - `curl http://localhost:3000/robots.txt` — verify contains `User-agent: *`, `Allow: /`, and `Sitemap:` line
    Kill the background server after verification.
  </verify>
  <done>
    `/sitemap.xml` returns valid XML listing all 6 pages. `/robots.txt` returns valid robots file with allow-all rule and sitemap reference.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add image priority comments in hero components and document ImagePlaceholder migration</name>
  <files>
    src/components/sections/HeroSection.tsx
    src/components/sections/HeroParallax.tsx
    src/components/sections/about/AboutHero.tsx
    src/components/sections/services/ServicesHero.tsx
    src/components/sections/products/ProductsHero.tsx
    src/components/sections/gallery/GalleryHero.tsx
    src/components/sections/contact/ContactHero.tsx
    src/components/ui/ImagePlaceholder.tsx
  </files>
  <action>
    **SEO-06 — Hero image priority prop:**
    1. In `src/components/ui/ImagePlaceholder.tsx`, add an optional `priority?: boolean` prop to the `ImagePlaceholderProps` interface. Render it as `data-priority={priority || undefined}` on the root `<div>`. This marks the element for the real-image migration — when replaced with `next/image`, the `priority` prop transfers directly.
    2. In each hero component that contains an `ImagePlaceholder` for the hero/background image, add `priority={true}` to the ImagePlaceholder usage. This applies to:
       - `HeroSection.tsx` / `HeroParallax.tsx` (homepage hero — check which one has the ImagePlaceholder)
       - `AboutHero.tsx`
       - `ServicesHero.tsx`
       - `ProductsHero.tsx`
       - `GalleryHero.tsx`
       - `ContactHero.tsx`
    3. Only add `priority={true}` to the ABOVE-THE-FOLD hero image placeholder — NOT to every ImagePlaceholder on the page. Below-fold placeholders should NOT get priority.

    **SEO-07 — Audit ImagePlaceholder call sites and add migration docs:**
    1. Run `grep -rn '<ImagePlaceholder' src/` to find every call site. Verify that EVERY usage has a non-empty `label` prop. If any call site is missing `label` or passes an empty string, fix it with a descriptive label (this maps to `alt` text during real-image migration).
    2. In `src/components/ui/ImagePlaceholder.tsx`, add a JSDoc block at the top of the component documenting the migration path:
    ```typescript
    /**
     * ImagePlaceholder — Temporary placeholder for real images.
     *
     * Migration guide (SEO-07):
     * When replacing with next/image:
     * 1. The `label` prop maps to the `alt` attribute
     * 2. Add `sizes` prop for responsive loading:
     *    - Hero images: sizes="100vw"
     *    - Grid cards (3-col): sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
     *    - Gallery items: sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
     * 3. Hero images: add priority={true} (only above-fold hero, not all images)
     * 4. Use fill={true} with object-cover for background-style images
     */
    ```

    **SEO-08 — Performance verification:**
    Run `npm run build` and check the build output for:
    - No "unoptimized images" warnings
    - Bundle sizes are reasonable (check for any chunks >250KB)
    - All pages are listed as static (ISR/SSG) not dynamic

    The site architecture is already well-optimized for performance (server components by default, LazyMotion, next/font with swap). The primary action here is confirming the build is clean and documenting the path forward for when real images arrive. Actual Lighthouse scoring requires a deployed build which is outside the scope of a dev build verification, but the 90+ target is realistic given the architecture.
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify no TypeScript errors. Run `npm run build` and confirm clean build output with no warnings. Verify `ImagePlaceholder.tsx` has `priority` prop in its interface. Verify each hero component passes `priority={true}` to its hero ImagePlaceholder. Run `grep -rn '<ImagePlaceholder' src/` and confirm every call site has a non-empty `label` prop. Verify `ImagePlaceholder.tsx` contains the SEO-07 migration guide JSDoc.
  </verify>
  <done>
    All 6 hero components pass `priority={true}` to their hero ImagePlaceholder. `ImagePlaceholder.tsx` has `priority` prop support and SEO-07 migration guide. Every ImagePlaceholder call site has a non-empty `label` prop. `npm run build` succeeds cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Lighthouse performance score on deployed preview</name>
  <files>N/A — human verification only</files>
  <action>
    What was built: SEO metadata, sitemap, robots.txt, image priority infrastructure, and clean production build from Plans 09-01 and 09-02.

    How to verify:
    1. Deploy to Vercel preview: run `vercel` CLI or push branch to trigger preview deployment
    2. Open the preview URL in Chrome
    3. Open DevTools → Lighthouse tab
    4. Run Lighthouse audit with "Performance" category selected, in "Desktop" mode
    5. Record the performance score — target is 90+
    6. If score is below 90, note the top diagnostics (e.g., "Reduce unused JavaScript", "Largest Contentful Paint")
    7. Optionally run Mobile mode — target is 80+ (lower due to CPU throttling and Framer Motion JS)

    Resume signal: Type "approved" with the Lighthouse score, or describe issues to address.
  </action>
  <verify>Human confirms Lighthouse desktop performance score of 90+ on deployed Vercel preview URL.</verify>
  <done>Lighthouse desktop performance score 90+ recorded and confirmed. SEO-08 requirement verified on production-equivalent build.</done>
</task>

</tasks>

<verification>
1. `curl /sitemap.xml` returns valid XML with 6 `<url>` entries containing all page URLs
2. `curl /robots.txt` returns `User-agent: *`, `Allow: /`, and `Sitemap: .../sitemap.xml`
3. `ImagePlaceholder.tsx` has `priority` prop in interface, renders as `data-priority`
4. All 6 hero components pass `priority={true}` to their hero ImagePlaceholder
5. Every `<ImagePlaceholder` call site has a non-empty `label` prop (verified by grep)
6. `ImagePlaceholder.tsx` contains SEO-07 migration guide with label-to-alt mapping and sizes patterns
7. `npm run build` succeeds cleanly with no warnings
8. Lighthouse performance score 90+ on deployed Vercel preview (verified in checkpoint)
</verification>

<success_criteria>
- `/sitemap.xml` returns valid XML listing all six pages with canonical URLs
- `/robots.txt` returns valid robots file allowing all crawlers and referencing the sitemap
- Hero components pass `priority={true}` to ImagePlaceholder, ready for next/image migration
- Every ImagePlaceholder call site has a non-empty `label` prop serving as future alt text
- ImagePlaceholder documents the migration path from placeholder to next/image with alt and sizes
- Production build succeeds cleanly
- Lighthouse performance score 90+ verified on a deployed Vercel preview URL
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-and-performance/09-02-SUMMARY.md`
</output>
