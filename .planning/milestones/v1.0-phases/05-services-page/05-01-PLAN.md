---
phase: 05-services-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/services.ts
  - src/components/sections/ServicesPreview.tsx
  - src/components/sections/services/ServicesHero.tsx
autonomous: true
requirements:
  - SERV-01
  - SERV-02
  - SERV-03
  - SERV-04
  - SERV-05
  - SERV-06
  - SERV-07

must_haves:
  truths:
    - "The services data file contains all six services with detailed descriptions"
    - "Snow Removal details mention contract, per-visit, and hourly pricing"
    - "Irrigation details mention licensed backflow testing and programmable controllers"
    - "The homepage ServicesPreview still shows exactly 3 service cards (Residential, Commercial, Municipal)"
    - "A ServicesHero component exists with the 'Our Services' headline"
  artifacts:
    - path: "src/lib/data/services.ts"
      provides: "All 6 services with details, imageLabel, featured fields"
      contains: "featured"
    - path: "src/components/sections/ServicesPreview.tsx"
      provides: "Homepage preview filtered to featured services only"
      contains: "featured"
    - path: "src/components/sections/services/ServicesHero.tsx"
      provides: "Interior page hero with Our Services headline"
      contains: "Our Services"
  key_links:
    - from: "src/components/sections/ServicesPreview.tsx"
      to: "src/lib/data/services.ts"
      via: "import + filter by featured"
      pattern: "services\\.filter.*featured"
---

<objective>
Expand the services data file from 3 to 6 services with rich detail descriptions, update the homepage ServicesPreview for backward compatibility, and create the ServicesHero component.

Purpose: Establishes the data layer that Plan 02 will consume for the service card grid, and ensures the homepage continues working correctly after the data expansion.
Output: Expanded services.ts, updated ServicesPreview.tsx, new ServicesHero.tsx
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-services-page/05-RESEARCH.md
@src/lib/data/services.ts
@src/components/sections/ServicesPreview.tsx
@src/components/sections/about/AboutHero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand services data file with all 6 services</name>
  <files>src/lib/data/services.ts</files>
  <action>
Expand the `Service` interface to add three new fields:
- `details: string` -- extended description for the Services page cards (2-4 sentences)
- `imageLabel: string` -- image placeholder label specific to the Services page card
- `featured: boolean` -- whether to show on the homepage ServicesPreview

Add `details`, `imageLabel`, and `featured` to the three existing services (Residential, Commercial, Municipal -- all `featured: true`). Update existing entry IDs and titles per requirements:
- `residential-landscaping` becomes `residential-maintenance` with title "Residential Maintenance"
- `commercial-landscaping` stays as-is
- `municipal-projects` stays as-is

Add three new service entries (all `featured: false`):
1. `landscape-design-build` -- "Landscape Design & Build". Details MUST mention: Pavestone, concrete, retaining walls, pool scapes, garden structures, design consultation to completion.
2. `irrigation-lighting` -- "Irrigation & Landscape Lighting". Details MUST mention: licensed backflow preventor testing, programmable controllers, lighting design, design/install/maintain.
3. `snow-removal` -- "Snow Removal". Details MUST mention: lot/drive clearing, salting, walkway shovelling, snow hauling, AND all three pricing options (contract, per-visit, hourly).

Array order must match the display order on the services page: Landscape Design & Build, Residential Maintenance, Commercial Landscaping, Municipal Projects, Irrigation & Landscape Lighting, Snow Removal.

Keep all existing fields (`id`, `title`, `description`, `image`, `href`) unchanged on the 3 original entries so the homepage ServicesPreview backward compat is preserved. The `description` field remains the SHORT version (homepage), `details` is the LONG version (services page).

Use the data content from the research file's "Expanded Services Data File" code example as a starting point, but verify all requirement-specific content is present.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no type errors. Grep for "featured" in services.ts to confirm the field exists. Grep for "backflow" and "contract" to confirm special content is present.</verify>
  <done>services.ts exports a `Service` interface with `details`, `imageLabel`, `featured` fields and a `services` array with exactly 6 entries. Snow Removal mentions all 3 pricing types. Irrigation mentions backflow testing.</done>
</task>

<task type="auto">
  <name>Task 2: Update ServicesPreview to filter by featured and create ServicesHero</name>
  <files>src/components/sections/ServicesPreview.tsx, src/components/sections/services/ServicesHero.tsx</files>
  <action>
**ServicesPreview.tsx update:**
Add a filter before the `.map()` call: `const featuredServices = services.filter(s => s.featured)`. Then map over `featuredServices` instead of `services`. This ensures only the 3 featured services (Residential, Commercial, Municipal) display on the homepage, even though the data array now has 6 entries. No other changes to the component -- all existing markup, styles, and behavior remain identical.

**ServicesHero.tsx creation:**
Create `src/components/sections/services/ServicesHero.tsx` as a Server Component (NO `'use client'`). Follow the exact pattern from `AboutHero.tsx`:
- `<section>` with `className="relative py-32 md:py-40 px-6 bg-forest overflow-hidden"`
- Absolute-positioned `ImagePlaceholder` background with label "Services — YWI crew working on a landscape project", className `"!aspect-auto h-full w-full !rounded-none !bg-forest-light/30"`
- `GrainOverlay` component
- `relative z-10` content wrapper with `max-w-4xl mx-auto text-center`
- `h1` with "Our Services" in `font-display text-4xl md:text-5xl lg:text-6xl text-cream mb-4`
- `p` subtitle: "From design and installation to year-round maintenance, we bring craftsmanship to every project." in `text-lg md:text-xl text-cream/80 max-w-2xl mx-auto`

Import `ImagePlaceholder` from `@/components/ui/ImagePlaceholder` and `GrainOverlay` from `@/components/ui/GrainOverlay`.
  </action>
  <verify>Run `npm run build` to confirm no build errors. Visit `http://localhost:3000` and confirm homepage still shows exactly 3 service cards (not 6). The new ServicesHero.tsx file should exist at the expected path.</verify>
  <done>Homepage ServicesPreview renders exactly 3 featured services. ServicesHero.tsx exists as a Server Component with "Our Services" h1, forest background, image placeholder, and grain overlay matching the AboutHero pattern.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no type errors
- `npm run build` succeeds
- Homepage at `/` shows exactly 3 service cards (Residential Maintenance, Commercial Landscaping, Municipal Projects) -- not 6
- `src/components/sections/services/ServicesHero.tsx` exists and contains "Our Services"
- `src/lib/data/services.ts` contains 6 service entries with `details`, `imageLabel`, and `featured` fields
- Snow Removal entry contains "contract", "per-visit", "hourly"
- Irrigation entry contains "backflow"
</verification>

<success_criteria>
The services data layer is complete with all 6 services, the homepage backward compatibility is preserved, and the ServicesHero component is ready for page assembly in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/05-services-page/05-01-SUMMARY.md`
</output>
