---
phase: 05-services-page
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/sections/services/ServiceCardGrid.tsx
  - src/components/sections/services/ServicesContact.tsx
  - src/app/services/page.tsx
autonomous: true
requirements:
  - SERV-08
  - SERV-09

must_haves:
  truths:
    - "All six service cards are visible on the Services page"
    - "Each service card shows an image placeholder, detailed description, and Get a Quote CTA"
    - "Cards have equal height within each row with CTA buttons aligned at the bottom"
    - "A bottom section says 'Not sure what you need? Let's talk.' with a link to /contact"
    - "The entire page is server-rendered with no new 'use client' boundaries"
  artifacts:
    - path: "src/components/sections/services/ServiceCardGrid.tsx"
      provides: "6-card responsive grid with image, description, CTA per card"
      contains: "Get a Quote"
    - path: "src/components/sections/services/ServicesContact.tsx"
      provides: "Bottom CTA section with contact prompt"
      contains: "Not sure what you need"
    - path: "src/app/services/page.tsx"
      provides: "Server Component page assembling all services sections"
      contains: "ServicesHero"
  key_links:
    - from: "src/components/sections/services/ServiceCardGrid.tsx"
      to: "src/lib/data/services.ts"
      via: "import services array and read details + imageLabel"
      pattern: "import.*services.*from.*lib/data/services"
    - from: "src/components/sections/services/ServiceCardGrid.tsx"
      to: "/contact"
      via: "Link href on each Get a Quote button"
      pattern: "href.*contact"
    - from: "src/components/sections/services/ServicesContact.tsx"
      to: "/contact"
      via: "Link href on Contact Us button"
      pattern: "href.*contact"
    - from: "src/app/services/page.tsx"
      to: "src/components/sections/services/"
      via: "imports ServicesHero, ServiceCardGrid, ServicesContact"
      pattern: "import.*from.*sections/services"
---

<objective>
Build the ServiceCardGrid (all 6 service cards) and ServicesContact (bottom CTA), then wire the complete Services page.

Purpose: Delivers the fully functional Services page where visitors can browse all services and find a clear path to request a quote.
Output: ServiceCardGrid.tsx, ServicesContact.tsx, completed page.tsx
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-services-page/05-RESEARCH.md
@.planning/phases/05-services-page/05-01-SUMMARY.md
@src/lib/data/services.ts
@src/components/sections/services/ServicesHero.tsx
@src/components/ui/AnimatedSection.tsx
@src/components/ui/ImagePlaceholder.tsx
@src/components/ui/GrainOverlay.tsx
@src/components/sections/CTABanner.tsx
@src/app/about/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServiceCardGrid component</name>
  <files>src/components/sections/services/ServiceCardGrid.tsx</files>
  <action>
Create `src/components/sections/services/ServiceCardGrid.tsx` as a Server Component (NO `'use client'`).

**Imports:**
- `Link` from `next/link`
- `{ services }` from `@/lib/data/services`
- `AnimatedSection` from `@/components/ui/AnimatedSection`
- `ImagePlaceholder` from `@/components/ui/ImagePlaceholder`

**Structure:**
- Outer `<section>` with `className="py-20 md:py-28 px-6"` (cream/default background -- no bg class)
- Inner `max-w-7xl mx-auto` container
- CSS Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8`
- Map over the full `services` array (all 6)

**Each card (wrapped in AnimatedSection with delay={index * 0.1}):**
- Outer div: `flex flex-col h-full rounded-lg overflow-hidden bg-white shadow-sm`
- `ImagePlaceholder` with `label={service.imageLabel}` and `aspectRatio="video"`
- Content div: `flex flex-col flex-1 p-6`
  - `h3` with `font-display text-xl md:text-2xl text-forest mb-3` containing `service.title`
  - `p` with `text-charcoal/70 leading-relaxed mb-6 flex-1` containing `service.details` (NOT `description` -- use the long version)
  - `Link` to `/contact` with text "Get a Quote", styled: `inline-flex items-center justify-center px-6 py-3 bg-terracotta hover:bg-terracotta-light text-cream font-medium rounded-lg transition-colors`

**Critical details:**
- Cards are NOT clickable links (unlike homepage ServicesPreview). Only the "Get a Quote" button is interactive.
- `h-full` on the card + `flex-1` on the description paragraph ensures equal-height cards with bottom-aligned CTAs.
- Use `service.details` (long description) NOT `service.description` (short homepage version).
- Use `service.imageLabel` for the ImagePlaceholder label (services-page-specific), NOT `service.image`.
- No hover lift effect on cards (`hover:shadow-lg hover:-translate-y-2` is for homepage cards that are links). A subtle `hover:shadow-md` is acceptable but not required.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no type errors. Grep for "Get a Quote" and "service.details" in the file to confirm correct content field usage.</verify>
  <done>ServiceCardGrid.tsx renders all 6 services as equal-height cards in a responsive 1/2/3-column grid. Each card has an image placeholder, detailed description, and "Get a Quote" Link to /contact.</done>
</task>

<task type="auto">
  <name>Task 2: Create ServicesContact and wire page.tsx</name>
  <files>src/components/sections/services/ServicesContact.tsx, src/app/services/page.tsx</files>
  <action>
**ServicesContact.tsx:**
Create `src/components/sections/services/ServicesContact.tsx` as a Server Component (NO `'use client'`). Follow the CTABanner pattern adapted for consultative messaging:

- `<section>` with `className="relative py-20 md:py-28 px-6 bg-forest overflow-hidden"` (forest background to bookend with hero)
- `GrainOverlay` component
- `AnimatedSection` with `className="relative z-10"`
- `max-w-3xl mx-auto text-center` container
- `h2` with `font-display text-3xl md:text-4xl text-cream mb-4`: "Not sure what you need?"
- `p` with `font-display text-xl md:text-2xl text-cream/80 mb-8`: "Let's talk." (use `Let&apos;s talk.` for the apostrophe)
- `p` with `text-cream/70 mb-10 max-w-xl mx-auto`: "Every property is different. Tell us about your space and we'll help you figure out the right approach." (use `we&apos;ll` for apostrophe)
- `Link` to `/contact` with text "Contact Us", styled: `inline-flex items-center px-8 py-4 bg-terracotta hover:bg-terracotta-light text-cream font-medium rounded-lg transition-colors`

Import `Link` from `next/link`, `AnimatedSection` from `@/components/ui/AnimatedSection`, `GrainOverlay` from `@/components/ui/GrainOverlay`.

**page.tsx:**
Replace the entire contents of `src/app/services/page.tsx` with the Server Component composition pattern (matching `app/about/page.tsx`):

```tsx
import ServicesHero from '@/components/sections/services/ServicesHero'
import ServiceCardGrid from '@/components/sections/services/ServiceCardGrid'
import ServicesContact from '@/components/sections/services/ServicesContact'

export default function ServicesPage() {
  return (
    <div>
      <ServicesHero />
      <ServiceCardGrid />
      <ServicesContact />
    </div>
  )
}
```

Page uses `<div>` root wrapper (NOT `<main>` -- root layout owns `<main>`). NO `'use client'` directive. This is a pure Server Component page.
  </action>
  <verify>Run `npm run build` to confirm full build succeeds. Run `npm run dev` and visit `http://localhost:3000/services` to confirm the page loads with hero, 6 service cards, and bottom CTA. Check page source to confirm server-rendered HTML (not a client shell).</verify>
  <done>ServicesContact.tsx displays "Not sure what you need? Let's talk." with a Contact Us link to /contact. page.tsx assembles ServicesHero + ServiceCardGrid + ServicesContact as a Server Component. The complete Services page is functional at /services.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- `/services` page shows: hero with "Our Services" -> 6 service cards in grid -> "Not sure what you need?" bottom CTA
- All 6 cards visible: Landscape Design & Build, Residential Maintenance, Commercial Landscaping, Municipal Projects, Irrigation & Landscape Lighting, Snow Removal
- Each card has: image placeholder (video aspect ratio), detailed description, "Get a Quote" button linking to /contact
- Snow Removal card mentions contract, per-visit, hourly pricing
- Irrigation card mentions licensed backflow testing
- Bottom CTA has "Not sure what you need?" heading, "Let's talk." subheading, and "Contact Us" link to /contact
- Cards have equal height within rows (CTA buttons aligned at bottom)
- Page is responsive: 1 col mobile, 2 col tablet, 3 col desktop
- No `'use client'` in any new files (only AnimatedSection wrapper provides client interactivity)
- Homepage at `/` still shows exactly 3 service preview cards
</verification>

<success_criteria>
The Services page is complete with all 6 service cards, proper CTAs, and the bottom contact prompt. A visitor can browse all services, see detailed descriptions, and find a clear path to request a quote via /contact.
</success_criteria>

<output>
After completion, create `.planning/phases/05-services-page/05-02-SUMMARY.md`
</output>
