---
phase: 18-content-section-images
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/images/services/landscape-design-build.jpg
  - public/images/services/residential-maintenance.jpg
  - public/images/services/commercial-landscaping.jpg
  - public/images/services/municipal-projects.jpg
  - public/images/services/irrigation-lighting.jpg
  - public/images/services/snow-removal.jpg
  - src/components/sections/services/ServiceCardGrid.tsx
  - src/components/sections/ServicesPreview.tsx
autonomous: true
requirements: [SERV-01]

must_haves:
  truths:
    - "Each of the 6 service cards on the Services page displays a real photograph matching its service type"
    - "The 3 featured service cards on the Homepage also display real photographs"
    - "All service card images render at 16:9 aspect ratio without distortion"
  artifacts:
    - path: "public/images/services/landscape-design-build.jpg"
      provides: "Landscape design photo (hardscaping/patio)"
    - path: "public/images/services/residential-maintenance.jpg"
      provides: "Residential lawn/garden maintenance photo"
    - path: "public/images/services/commercial-landscaping.jpg"
      provides: "Commercial property grounds photo"
    - path: "public/images/services/municipal-projects.jpg"
      provides: "Public park/community space photo"
    - path: "public/images/services/irrigation-lighting.jpg"
      provides: "Irrigation sprinkler or landscape lighting photo"
    - path: "public/images/services/snow-removal.jpg"
      provides: "Snow plowing/clearing photo"
    - path: "src/components/sections/services/ServiceCardGrid.tsx"
      provides: "Service cards with next/image instead of ImagePlaceholder"
      contains: "next/image"
    - path: "src/components/sections/ServicesPreview.tsx"
      provides: "Homepage service previews with next/image"
      contains: "next/image"
  key_links:
    - from: "src/components/sections/services/ServiceCardGrid.tsx"
      to: "src/lib/data/services.ts"
      via: "service.image path used as next/image src"
      pattern: "src=.service\\.image."
    - from: "src/components/sections/ServicesPreview.tsx"
      to: "src/lib/data/services.ts"
      via: "service.image path used as next/image src"
      pattern: "src=.service\\.image."
---

<objective>
Download 6 sample service photographs and replace ImagePlaceholder with next/image in the ServiceCardGrid and ServicesPreview components.

Purpose: Service cards currently show colored placeholder boxes. Real photographs matching each service type (design/build, residential, commercial, municipal, irrigation, snow removal) make the services feel tangible and professional.

Output: 6 JPEG images in public/images/services/ and 2 updated components using next/image.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-image-infrastructure/16-01-SUMMARY.md
@.planning/phases/17-hero-images/17-02-SUMMARY.md
@src/lib/data/services.ts
@src/components/sections/services/ServiceCardGrid.tsx
@src/components/sections/ServicesPreview.tsx
@src/components/ui/ImagePlaceholder.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download 6 sample service images</name>
  <files>
    public/images/services/landscape-design-build.jpg
    public/images/services/residential-maintenance.jpg
    public/images/services/commercial-landscaping.jpg
    public/images/services/municipal-projects.jpg
    public/images/services/irrigation-lighting.jpg
    public/images/services/snow-removal.jpg
  </files>
  <action>
    Download 6 sample landscape/service photographs from picsum.photos (or any free image source that returns valid JPEG data directly). Each image should be approximately 800x450 (16:9 aspect ratio, suitable for card display — NOT full-viewport like hero images).

    File naming MUST match the IDs in src/lib/data/services.ts exactly:
    - landscape-design-build.jpg — search for: hardscape patio, stone work, retaining wall
    - residential-maintenance.jpg — search for: well-maintained home garden, mowed lawn
    - commercial-landscaping.jpg — search for: commercial property, office grounds, maintained beds
    - municipal-projects.jpg — search for: public park, community green space, streetscape
    - irrigation-lighting.jpg — search for: sprinkler system, garden irrigation, landscape lighting
    - snow-removal.jpg — search for: snow plow, cleared parking lot, winter maintenance

    After download, verify each file with `file` command to confirm valid JPEG image data (not HTML redirects). Delete and re-download from alternative source if any file is not a valid JPEG.

    Use picsum.photos pattern: `curl -L -o public/images/services/{id}.jpg "https://picsum.photos/800/450"` — each call returns a different random image. This is acceptable since these are sample placeholders the owner will replace.
  </action>
  <verify>
    Run: `file public/images/services/*.jpg` — all 6 must report "JPEG image data".
    Run: `ls -la public/images/services/*.jpg` — all 6 files present, each > 10KB.
  </verify>
  <done>6 valid JPEG images exist in public/images/services/, one per service ID from services.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ImagePlaceholder with next/image in ServiceCardGrid and ServicesPreview</name>
  <files>
    src/components/sections/services/ServiceCardGrid.tsx
    src/components/sections/ServicesPreview.tsx
  </files>
  <action>
    In both components, replace `ImagePlaceholder` with `next/image`. Follow the pattern established in Phase 17 hero components.

    **ServiceCardGrid.tsx:**
    1. Remove `import ImagePlaceholder from '@/components/ui/ImagePlaceholder'`
    2. Add `import Image from 'next/image'`
    3. Replace the `<ImagePlaceholder label={service.imageLabel} aspectRatio="video" />` with:
       ```tsx
       <div className="relative aspect-video">
         <Image
           src={service.image}
           alt={service.imageLabel}
           fill
           sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 33vw"
           className="object-cover"
         />
       </div>
       ```
       The wrapping div provides the aspect ratio container for fill mode. sizes matches the 3-column grid layout (full on mobile, half on md, third on lg).

    **ServicesPreview.tsx:**
    1. Remove `import ImagePlaceholder from '@/components/ui/ImagePlaceholder'`
    2. Add `import Image from 'next/image'`
    3. Replace the `<ImagePlaceholder label={service.image} aspectRatio="video" />` with:
       ```tsx
       <div className="relative aspect-video">
         <Image
           src={service.image}
           alt={service.title}
           fill
           sizes="(max-width: 768px) 100vw, 33vw"
           className="object-cover"
         />
       </div>
       ```
       The alt text uses service.title since ServicesPreview passes service.image (the path) as the label — the title is the correct semantic alt text. sizes matches the md:grid-cols-3 layout.

    Do NOT add `priority` prop — these are below-fold card images, not above-fold heroes.
  </action>
  <verify>
    Run: `npx next build 2>&1 | tail -20` — build succeeds with zero errors.
    Run: `grep -c "ImagePlaceholder" src/components/sections/services/ServiceCardGrid.tsx src/components/sections/ServicesPreview.tsx` — both return 0.
    Run: `grep -c "next/image" src/components/sections/services/ServiceCardGrid.tsx src/components/sections/ServicesPreview.tsx` — both return at least 1.
  </verify>
  <done>ServiceCardGrid and ServicesPreview both render service photos via next/image. No ImagePlaceholder imports remain in either file. Build passes.</done>
</task>

</tasks>

<verification>
1. `file public/images/services/*.jpg` — all 6 report valid JPEG
2. `grep -r "ImagePlaceholder" src/components/sections/services/ServiceCardGrid.tsx src/components/sections/ServicesPreview.tsx` — no matches
3. `npx next build` — zero errors
</verification>

<success_criteria>
- 6 valid JPEG images exist in public/images/services/, named to match service IDs
- ServiceCardGrid.tsx uses next/image with fill mode and responsive sizes
- ServicesPreview.tsx uses next/image with fill mode and responsive sizes
- Zero ImagePlaceholder imports in either component
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-content-section-images/18-01-SUMMARY.md`
</output>
