---
phase: 18-content-section-images
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/images/team/member-1.jpg
  - public/images/team/member-2.jpg
  - public/images/team/member-3.jpg
  - public/images/team/member-4.jpg
  - src/components/sections/about/TeamSection.tsx
  - src/components/sections/AboutTeaser.tsx
autonomous: true
requirements: [TEAM-01]

must_haves:
  truths:
    - "Each of the 4 team member cards on the About page displays a professional portrait-style photo"
    - "The About teaser on the Homepage displays a real team/project photo"
    - "All team portrait images render at 3:4 portrait aspect ratio without distortion"
  artifacts:
    - path: "public/images/team/member-1.jpg"
      provides: "Portrait photo for Owner / Lead Designer"
    - path: "public/images/team/member-2.jpg"
      provides: "Portrait photo for Project Manager"
    - path: "public/images/team/member-3.jpg"
      provides: "Portrait photo for Lead Installer"
    - path: "public/images/team/member-4.jpg"
      provides: "Portrait photo for Irrigation Specialist"
    - path: "src/components/sections/about/TeamSection.tsx"
      provides: "Team member cards with next/image instead of ImagePlaceholder"
      contains: "next/image"
    - path: "src/components/sections/AboutTeaser.tsx"
      provides: "Homepage about teaser with next/image team photo"
      contains: "next/image"
  key_links:
    - from: "src/components/sections/about/TeamSection.tsx"
      to: "src/lib/data/about.ts"
      via: "member.image path used as next/image src"
      pattern: "src=.member\\.image."
    - from: "src/components/sections/AboutTeaser.tsx"
      to: "public/images/team/"
      via: "hardcoded team photo path as next/image src"
      pattern: "src=./images/team/"
---

<objective>
Download 4 sample team portrait photographs and 1 team project photo, then replace ImagePlaceholder with next/image in the TeamSection and AboutTeaser components.

Purpose: Team member cards currently show colored placeholder boxes. Professional portrait-style photos humanize the company, build trust, and let visitors see the real people behind the work. The AboutTeaser photo on the homepage anchors the "Rooted in Fergus" story.

Output: 4 portrait JPEG images in public/images/team/, and 2 updated components using next/image.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-image-infrastructure/16-01-SUMMARY.md
@src/lib/data/about.ts
@src/components/sections/about/TeamSection.tsx
@src/components/sections/AboutTeaser.tsx
@src/components/ui/ImagePlaceholder.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download 4 sample team portrait images and 1 team project photo</name>
  <files>
    public/images/team/member-1.jpg
    public/images/team/member-2.jpg
    public/images/team/member-3.jpg
    public/images/team/member-4.jpg
  </files>
  <action>
    Download 4 portrait-orientation photographs from picsum.photos (or any free source returning valid JPEG data). Each image should be approximately 450x600 (3:4 portrait aspect ratio, suitable for team member cards).

    File naming MUST match the IDs in src/lib/data/about.ts exactly:
    - member-1.jpg — Owner / Lead Designer portrait
    - member-2.jpg — Project Manager portrait
    - member-3.jpg — Lead Installer portrait
    - member-4.jpg — Irrigation Specialist portrait

    These are sample placeholders. The owner will replace them with actual team photos before launch.

    After download, verify each file with `file` command to confirm valid JPEG image data. Delete and re-download if any file is not a valid JPEG.

    Use picsum.photos pattern: `curl -L -o public/images/team/{id}.jpg "https://picsum.photos/450/600"` — each call returns a different random image.

    Note: The AboutTeaser component currently hardcodes the label "YWI team on a residential project site" — it does not reference a data file. For this image, use one of the existing team member photos or download a separate landscape-oriented team photo. Since the AboutTeaser uses aspectRatio="square", download one additional 600x600 image as `public/images/team/team-project.jpg` for use in the AboutTeaser component.
  </action>
  <verify>
    Run: `file public/images/team/*.jpg` — all files must report "JPEG image data".
    Run: `ls -la public/images/team/*.jpg` — 5 files present (member-1 through member-4 + team-project), each > 10KB.
  </verify>
  <done>4 valid portrait JPEG images + 1 team project photo exist in public/images/team/.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ImagePlaceholder with next/image in TeamSection and AboutTeaser</name>
  <files>
    src/components/sections/about/TeamSection.tsx
    src/components/sections/AboutTeaser.tsx
  </files>
  <action>
    In both components, replace `ImagePlaceholder` with `next/image`.

    **TeamSection.tsx:**
    1. Remove `import ImagePlaceholder from '@/components/ui/ImagePlaceholder'`
    2. Add `import Image from 'next/image'`
    3. Replace the `<ImagePlaceholder label={member.image} aspectRatio="portrait" className="rounded-xl mb-4" />` with:
       ```tsx
       <div className="relative aspect-[3/4] rounded-xl overflow-hidden mb-4">
         <Image
           src={member.image}
           alt={`${member.name} — ${member.role}`}
           fill
           sizes="(max-width: 768px) 50vw, 25vw"
           className="object-cover"
         />
       </div>
       ```
       The wrapping div provides the 3:4 portrait aspect ratio with rounded corners and overflow hidden (replacing the className that was on ImagePlaceholder). The alt text combines name and role for accessibility. sizes matches the grid-cols-2 md:grid-cols-4 layout.

    **AboutTeaser.tsx:**
    1. Remove `import ImagePlaceholder from '@/components/ui/ImagePlaceholder'`
    2. Add `import Image from 'next/image'`
    3. Replace the `<ImagePlaceholder label="YWI team on a residential project site" aspectRatio="square" className="rounded-xl" />` with:
       ```tsx
       <div className="relative aspect-square rounded-xl overflow-hidden">
         <Image
           src="/images/team/team-project.jpg"
           alt="YWI team on a residential project site"
           fill
           sizes="(max-width: 768px) 100vw, 50vw"
           className="object-cover"
         />
       </div>
       ```
       The alt text preserves the original label. sizes matches the md:grid-cols-2 layout (full on mobile, half on md+).

    Do NOT add `priority` prop — these are below-fold content images.
  </action>
  <verify>
    Run: `npx next build 2>&1 | tail -20` — build succeeds with zero errors.
    Run: `grep -c "ImagePlaceholder" src/components/sections/about/TeamSection.tsx src/components/sections/AboutTeaser.tsx` — both return 0.
    Run: `grep -c "next/image" src/components/sections/about/TeamSection.tsx src/components/sections/AboutTeaser.tsx` — both return at least 1.
  </verify>
  <done>TeamSection and AboutTeaser both render real photos via next/image. No ImagePlaceholder imports remain in either file. Build passes.</done>
</task>

</tasks>

<verification>
1. `file public/images/team/*.jpg` — all report valid JPEG
2. `grep -r "ImagePlaceholder" src/components/sections/about/TeamSection.tsx src/components/sections/AboutTeaser.tsx` — no matches
3. `npx next build` — zero errors
</verification>

<success_criteria>
- 4 valid portrait JPEG images + 1 team project photo exist in public/images/team/
- TeamSection.tsx uses next/image with fill mode and responsive sizes
- AboutTeaser.tsx uses next/image with fill mode, hardcoded team project image path
- Zero ImagePlaceholder imports in either component
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-content-section-images/18-03-SUMMARY.md`
</output>
