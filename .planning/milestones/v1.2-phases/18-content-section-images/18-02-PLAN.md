---
phase: 18-content-section-images
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/images/products/aggregates.jpg
  - public/images/products/mulch.jpg
  - public/images/products/fertilizer.jpg
  - public/images/products/natural-stone.jpg
  - public/images/products/topsoil.jpg
  - public/images/products/interlock.jpg
  - public/images/products/seed.jpg
  - src/components/sections/products/ProductCategoryGrid.tsx
  - src/components/sections/ProductsBanner.tsx
autonomous: true
requirements: [PROD-01]

must_haves:
  truths:
    - "Each of the 7 product category cards on the Products page displays a real photograph of that material type"
    - "The 7 product thumbnails on the Homepage products banner also display real photographs"
    - "All product card images render at 1:1 square aspect ratio without distortion"
  artifacts:
    - path: "public/images/products/aggregates.jpg"
      provides: "Gravel/crushed stone photo"
    - path: "public/images/products/mulch.jpg"
      provides: "Bark mulch/wood chips photo"
    - path: "public/images/products/fertilizer.jpg"
      provides: "Fertilizer granules/bags photo"
    - path: "public/images/products/natural-stone.jpg"
      provides: "Flagstone/armour stone photo"
    - path: "public/images/products/topsoil.jpg"
      provides: "Topsoil/garden soil photo"
    - path: "public/images/products/interlock.jpg"
      provides: "Interlocking pavers photo"
    - path: "public/images/products/seed.jpg"
      provides: "Grass seed photo"
    - path: "src/components/sections/products/ProductCategoryGrid.tsx"
      provides: "Product cards with next/image instead of ImagePlaceholder"
      contains: "next/image"
    - path: "src/components/sections/ProductsBanner.tsx"
      provides: "Homepage product thumbnails with next/image"
      contains: "next/image"
  key_links:
    - from: "src/components/sections/products/ProductCategoryGrid.tsx"
      to: "src/lib/data/products.ts"
      via: "product.image path used as next/image src"
      pattern: "src=.product\\.image."
    - from: "src/components/sections/ProductsBanner.tsx"
      to: "src/lib/data/products.ts"
      via: "product.image path used as next/image src"
      pattern: "src=.product\\.image."
---

<objective>
Download 7 sample product material photographs and replace ImagePlaceholder with next/image in the ProductCategoryGrid and ProductsBanner components.

Purpose: Product cards currently show colored placeholder boxes. Real photographs of each material type (aggregates, mulch, fertilizer, stone, topsoil, interlock, seed) let customers see what they are buying and reinforce the retail yard's inventory credibility.

Output: 7 JPEG images in public/images/products/ and 2 updated components using next/image.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-image-infrastructure/16-01-SUMMARY.md
@src/lib/data/products.ts
@src/components/sections/products/ProductCategoryGrid.tsx
@src/components/sections/ProductsBanner.tsx
@src/components/ui/ImagePlaceholder.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download 7 sample product images</name>
  <files>
    public/images/products/aggregates.jpg
    public/images/products/mulch.jpg
    public/images/products/fertilizer.jpg
    public/images/products/natural-stone.jpg
    public/images/products/topsoil.jpg
    public/images/products/interlock.jpg
    public/images/products/seed.jpg
  </files>
  <action>
    Download 7 sample product/material photographs from picsum.photos (or any free source returning valid JPEG data). Each image should be approximately 600x600 (1:1 square aspect ratio, suitable for product card display).

    File naming MUST match the IDs in src/lib/data/products.ts exactly:
    - aggregates.jpg — represents: gravel, crushed stone, drainage materials
    - mulch.jpg — represents: bark mulch, wood chips
    - fertilizer.jpg — represents: lawn/garden fertilizer
    - natural-stone.jpg — represents: flagstone, armour stone, boulders
    - topsoil.jpg — represents: screened topsoil, garden soil
    - interlock.jpg — represents: interlocking pavers
    - seed.jpg — represents: grass seed mix

    After download, verify each file with `file` command to confirm valid JPEG image data. Delete and re-download if any file is not a valid JPEG.

    Use picsum.photos pattern: `curl -L -o public/images/products/{id}.jpg "https://picsum.photos/600/600"` — each call returns a different random image. Acceptable since these are sample placeholders.
  </action>
  <verify>
    Run: `file public/images/products/*.jpg` — all 7 must report "JPEG image data".
    Run: `ls -la public/images/products/*.jpg` — all 7 files present, each > 10KB.
  </verify>
  <done>7 valid JPEG images exist in public/images/products/, one per product ID from products.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ImagePlaceholder with next/image in ProductCategoryGrid and ProductsBanner</name>
  <files>
    src/components/sections/products/ProductCategoryGrid.tsx
    src/components/sections/ProductsBanner.tsx
  </files>
  <action>
    In both components, replace `ImagePlaceholder` with `next/image`. Follow the pattern established in Phase 17.

    **ProductCategoryGrid.tsx:**
    1. Remove `import ImagePlaceholder from '@/components/ui/ImagePlaceholder'`
    2. Add `import Image from 'next/image'`
    3. Replace the `<ImagePlaceholder label={product.image} aspectRatio="square" />` with:
       ```tsx
       <Image
         src={product.image}
         alt={product.name}
         fill
         sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw"
         className="object-cover"
       />
       ```
       The parent `<div className="relative">` already exists. Add `aspect-square` to this parent div if not already present so the fill image has a sized container. The sizes prop matches the responsive grid: full on mobile, 2-col on sm, 3-col on lg, 4-col on xl.
    4. Use `product.name` as alt text (e.g., "Aggregates", "Mulch") since `product.image` was the path string passed as label — the name is the correct semantic alt text.

    **ProductsBanner.tsx:**
    1. Remove `import ImagePlaceholder from '@/components/ui/ImagePlaceholder'`
    2. Add `import Image from 'next/image'`
    3. Replace the `<ImagePlaceholder label={product.image} aspectRatio="square" className="!bg-sage/20" />` with:
       ```tsx
       <div className="relative aspect-square">
         <Image
           src={product.image}
           alt={product.name}
           fill
           sizes="(max-width: 640px) 50vw, (max-width: 1024px) 33vw, 25vw"
           className="object-cover"
         />
       </div>
       ```
       sizes matches the grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 layout. The wrapping div provides aspect ratio for fill mode.

    Do NOT add `priority` prop — these are not above-fold hero images.
  </action>
  <verify>
    Run: `npx next build 2>&1 | tail -20` — build succeeds with zero errors.
    Run: `grep -c "ImagePlaceholder" src/components/sections/products/ProductCategoryGrid.tsx src/components/sections/ProductsBanner.tsx` — both return 0.
    Run: `grep -c "next/image" src/components/sections/products/ProductCategoryGrid.tsx src/components/sections/ProductsBanner.tsx` — both return at least 1.
  </verify>
  <done>ProductCategoryGrid and ProductsBanner both render product photos via next/image. No ImagePlaceholder imports remain in either file. Build passes.</done>
</task>

</tasks>

<verification>
1. `file public/images/products/*.jpg` — all 7 report valid JPEG
2. `grep -r "ImagePlaceholder" src/components/sections/products/ProductCategoryGrid.tsx src/components/sections/ProductsBanner.tsx` — no matches
3. `npx next build` — zero errors
</verification>

<success_criteria>
- 7 valid JPEG images exist in public/images/products/, named to match product IDs
- ProductCategoryGrid.tsx uses next/image with fill mode and responsive sizes
- ProductsBanner.tsx uses next/image with fill mode and responsive sizes
- Zero ImagePlaceholder imports in either component
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-content-section-images/18-02-SUMMARY.md`
</output>
