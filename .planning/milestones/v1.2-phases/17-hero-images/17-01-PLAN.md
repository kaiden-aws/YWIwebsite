---
phase: 17-hero-images
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HeroParallax.tsx
  - public/images/heroes/home-hero.jpg
autonomous: true
requirements: [HERO-01]

must_haves:
  truths:
    - "Home page hero displays a real landscaping photo as full-viewport parallax background"
    - "Home page hero image loads immediately without lazy loading delay (priority)"
    - "No layout shift occurs when the hero image loads"
    - "Parallax scroll effect still works on the hero background"
    - "Reduced motion preference still disables parallax"
  artifacts:
    - path: "src/components/sections/HeroParallax.tsx"
      provides: "Parallax hero with next/image replacing ImagePlaceholder"
      contains: "next/image"
    - path: "public/images/heroes/home-hero.jpg"
      provides: "Home page hero photograph"
  key_links:
    - from: "src/components/sections/HeroParallax.tsx"
      to: "/images/heroes/home-hero.jpg"
      via: "next/image src prop"
      pattern: "src=.*heroes/home-hero"
---

<objective>
Replace the ImagePlaceholder in the home page parallax hero with a real landscaping photograph rendered via next/image.

Purpose: The home page hero is the single most impactful visual on the entire site. Replacing the colored placeholder box with a real full-viewport photo immediately communicates professionalism and quality.

Output: HeroParallax.tsx uses next/image with fill mode, priority loading, and preserved parallax behavior.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-image-infrastructure/16-01-SUMMARY.md
@src/components/sections/HeroParallax.tsx
@src/components/sections/HeroSection.tsx
@src/components/ui/ImagePlaceholder.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download sample hero image and replace ImagePlaceholder in HeroParallax</name>
  <files>
    src/components/sections/HeroParallax.tsx
    public/images/heroes/home-hero.jpg
  </files>
  <action>
1. Download a sample landscaping hero image to `public/images/heroes/home-hero.jpg`. Use a high-quality landscape-oriented photo showing a completed landscaping project (garden, lawn, hardscape, or outdoor living space). The image should be at least 1920px wide for full-viewport coverage. Use curl or wget to download from a free stock photo source (e.g., Unsplash or Pexels direct download URL). If download fails, create a simple placeholder JPEG using ImageMagick or a similar CLI tool — any real image file is acceptable for this phase since the owner will replace with actual project photos later.

2. Edit `src/components/sections/HeroParallax.tsx`:
   - Remove the `import ImagePlaceholder` line
   - Add `import Image from 'next/image'`
   - Replace the `<ImagePlaceholder>` block inside the parallax `m.div` with:
     ```tsx
     <Image
       src="/images/heroes/home-hero.jpg"
       alt="Completed landscape project showcasing professional outdoor design"
       fill
       priority
       sizes="100vw"
       className="object-cover"
       quality={85}
     />
     ```
   - Keep the `m.div` wrapper with `style={{ y }}` for parallax — the Image is a child inside it
   - Remove the wrapping `<div className="h-full w-full">` that was around ImagePlaceholder — next/image with fill handles sizing via its parent
   - Ensure the parallax `m.div` parent has `className="absolute inset-0 -z-10"` (it already does) — this is the positioning context for the fill image
   - The dark overlay for text readability comes from GrainOverlay in HeroSection.tsx — no additional overlay needed

3. Verify the file has no TypeScript errors and the ImagePlaceholder import is fully removed.
  </action>
  <verify>
Run `npx next build` (or `npm run build`) — should complete with zero errors. Visually confirm by running `npm run dev` and loading http://localhost:3000 — the hero should show a real image with parallax scroll effect instead of a gray placeholder box.
  </verify>
  <done>
Home page hero at / displays a real landscaping photograph as a full-viewport parallax background. The image loads with priority (no lazy loading delay). No layout shift occurs. Parallax scroll effect is preserved. The ImagePlaceholder import is removed from HeroParallax.tsx.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero TypeScript errors
2. Home page (/) shows a real photograph as the hero background, not a colored placeholder
3. Scrolling the page causes the hero image to parallax (move slower than foreground content)
4. The hero image is rendered with `<img>` tag containing `loading="eager"` and `fetchpriority="high"` (from next/image priority prop)
5. No cumulative layout shift on the hero section (fill mode with absolute parent prevents CLS)
</verification>

<success_criteria>
- Real landscaping photo visible as home page hero background
- Parallax scroll effect functional
- priority loading active (no lazy load)
- Zero build errors
- No ImagePlaceholder usage in HeroParallax.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/17-hero-images/17-01-SUMMARY.md`
</output>
